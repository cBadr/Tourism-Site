# ⚠️ مسودة ملغاة — لا تُعتمد

> **هذا الملف مسودة أولية كُتبت قبل استلام رؤية بدر الكاملة، وقد أُلغيت.**
> المصدر الأعلى للحقيقة الآن: `docs/VISION.md` (رؤية بدر الأصلية كاملة).
> خارطة الطريق المعتمدة ستُبنى في `docs/ROADMAP.md`.
> ما يصلح من هذه المسودة سيُنقل إلى خارطة الطريق؛ وأي تعارض بينها وبين الرؤية → الرؤية هي الحاكمة.

---

# خطة العمل — موقع خدمات النقل السياحي (Tours-01) [ملغاة]

---

## ١. الرؤية والنموذج التجاري

- منصة نقل سياحي في مصر، التنفيذ عبر **متعهدين** (white-label — العميل لا يعرف المتعهد)، والربح هو **فارق السعر** بين ما يدفعه العميل وتكلفة المتعهد.
- سيارة بدر خارج التشغيل — احتياطي للطوارئ وإنقاذ الرحلات فقط.
- الموقع عربي RTL بالكامل، والتسعير بالجنيه المصري.
- السيو جزء أصيل من المنتج: صفحة محتوى حقيقية لكل مسار مُختار.

## ٢. سجل القرارات المحسومة (لا يُعاد فتحها إلا بقرار صريح)

| # | القرار | الاختيار |
|---|--------|----------|
| 1 | نموذج الطلب | **حجز فوري بتسعير آلي** + «اطلب عرض سعر» لما هو خارج النطاق |
| 2 | محرك التسعير | **تعريفة كيلومتر لكل فئة سيارة + أسعار مخصصة لمسارات تعلو عليها** |
| 3 | المسافات | **تلقائي**: Google Maps API أساسي + بديل مجاني (OSRM) احتياطي + **كاش دائم في قاعدة البيانات** |
| 4 | أنواع الرحلات | **توصيلة اتجاه واحد + ذهاب وعودة** (الإيجار اليومي والجولات → «اطلب عرض سعر» مؤقتاً) |
| 5 | الدفع | **تحويل يدوي موثّق** (رفع إيصال + تأكيد من اللوحة) — بوابة الدفع بعد الإطلاق |
| 6 | مبلغ الدفع | خياران للعميل: **كامل المبلغ** أو **حد أدنى (عربون)** والباقي مع السائق — النسب تُدار من اللوحة |
| 7 | حساب العميل | **حجز كضيف** (اسم + موبايل/واتساب) + تسجيل اختياري بخصم بسيط |
| 8 | لوحة التحكم | كاملة: طلبات + محتوى + متعهدين + **حسابات كاملة** (هامش لكل رحلة، مقاصة متعهدين، تقارير شهرية) |
| 9 | توقيت الحسابات | **كلها قبل الإطلاق** — لا إطلاق بدون المقاصة والتقارير |
| 10 | اللغات | **عربي فقط** — البنية تسمح بإضافة لغات لاحقاً بدون إعادة بناء |
| 11 | النطاق الجغرافي | **مسارات مختارة** (~١٠–٢٠ مساراً شهيراً) تزيد تدريجياً من اللوحة |
| 12 | الإشعارات | لوحة التحكم + **بوت تليجرام** + **إيميل** لكل طلب/حجز جديد |

## ٣. الحزمة التقنية (Stack)

نفس نمط مشاريع بدر السابقة لنقل الخبرة وإعادة استخدام الأنماط:

- **Next.js 16** (App Router) + React 19 + TypeScript
- **Tailwind CSS v4** + shadcn/ui — عربي RTL افتراضاً
- **Supabase**: Postgres 17 + Auth + Storage (لإيصالات التحويل وصور المحتوى)
- **Vercel** للاستضافة
- **مبدأ ثابت من منصة الإدارة:** كل الحسابات المالية Postgres-first — دوال و views في قاعدة البيانات، لا حسابات مالية في JavaScript
- **مبدأ أمني ثابت:** RLS على كل الجداول + الانتباه للفخّين المعروفين (منح `anon` من مصدرين؛ و`UPDATE`/`DELETE` تنجح بصفر صفوف عند رفض RLS)

## ٤. نموذج البيانات (الجداول الرئيسية)

**المرجعية والتسعير**
- `vehicle_classes` — فئات السيارات (سيدان، ميني فان، ميكروباص، كوستر، أتوبيس...) بالسعة والمزايا
- `locations` — النقاط المعروفة (مطارات، مدن، مناطق) بإحداثياتها
- `routes` — المسارات المختارة: نقطتان + **مسافة مخزنة** + slug + محتوى صفحة السيو
- `tariffs` — تعريفة الكيلومتر لكل فئة + رسم أساسي + **حد أدنى لسعر الرحلة** + معامل الذهاب والعودة
- `route_prices` — أسعار مخصصة (مسار × فئة) تعلو على التعريفة
- `distance_cache` — مسافات محسوبة من الـ API لأي زوج نقاط (تُحسب مرة وتُحفظ للأبد)

**الطلبات والحجوزات**
- `customers` — عملاء (ضيوف بسجل تلقائي بالموبايل + حسابات مسجلة بخصم)
- `bookings` — الحجوزات: المسار/النقاط، التاريخ، الفئة، النوع (اتجاه/ذهاب وعودة)، السعر المحسوب، حالة الحجز
- `quote_requests` — طلبات عرض السعر (خارج المسارات المسعّرة أو إيجار يومي/جولات)
- `payments` — التحويلات: المبلغ، النوع (كامل/عربون)، صورة الإيصال، حالة التحقق

**التشغيل والمحاسبة**
- `subcontractors` — المتعهدون وبيانات التواصل
- `trip_assignments` — إسناد كل حجز مؤكد لمتعهد + **تكلفته المتفق عليها** (الهامش = الإيراد − التكلفة، يُحسب في Postgres)
- `settlements` — المقاصة الشهرية لكل متعهد
- `settings` — إعدادات عامة: نسبة العربون، الحد الأدنى، نسبة خصم التسجيل، بيانات التحويل...

**دورة حالة الحجز:**
`جديد ← بانتظار الدفع ← بانتظار التحقق ← مؤكد ← مُسند لمتعهد ← منفذ ← (ملغي في أي مرحلة)`

## ٥. محرك التسعير — `quote_price()` دالة Postgres واحدة

```
السعر = max(
  الحد الأدنى للفئة,
  الرسم الأساسي + المسافة كم × تعريفة الكيلومتر للفئة
) × (معامل الذهاب والعودة إن وُجد)
− خصم العميل المسجل (إن وُجد)
```

- لو للمسار سعر مخصص في `route_prices` → يعلو على المعادلة.
- ترتيب جلب المسافة: `routes` المخزنة ← `distance_cache` ← Google Maps API ← OSRM احتياطي ← فشل الكل؟ يتحول الطلب تلقائياً لـ«اطلب عرض سعر».
- كل استعلام API جديد يُحفظ في الكاش — فاتورة الـ API تتناقص مع الوقت بدل ما تزيد.

## ٦. مراحل التنفيذ

> كل مرحلة لها «تعريف اكتمال» — لا ننتقل للتالية قبل إنجازه.

### المرحلة ٠ — الأساس
Scaffold للمشروع (Next.js 16 + Tailwind v4 + shadcn/ui بثيم RTL عربي)، مشروع Supabase، الـ schema الأساسي بالكامل مع RLS، هوية بصرية أولية (ألوان/خط عربي).
**اكتمال:** المشروع يبني بدون أخطاء، قاعدة البيانات كاملة بالجداول والدوال الفارغة، صفحة رئيسية هيكلية.

### المرحلة ١ — الموقع العام (المحتوى)
الرئيسية، صفحات الخدمات، قائمة المسارات + صفحة لكل مسار (المحتوى من قاعدة البيانات)، من نحن، تواصل معنا، زر واتساب عائم.
**اكتمال:** موقع كامل قابل للتصفح بمحتوى حقيقي لـ ٥ مسارات على الأقل.

### المرحلة ٢ — محرك التسعير والحجز
دالة `quote_price()` + تكامل المسافات (Google + OSRM + كاش)، نموذج الحجز (ضيف)، شاشة السعر الفوري، إنشاء الحجز، نموذج «اطلب عرض سعر».
**اكتمال:** عميل يختار نقطتين وتاريخاً وفئة → يشوف سعراً صحيحاً → يُنشئ حجزاً يظهر في قاعدة البيانات.

### المرحلة ٣ — الدفع اليدوي الموثّق
اختيار كامل/عربون، عرض بيانات التحويل، رفع صورة الإيصال (Supabase Storage)، صفحة متابعة الحجز للعميل برابط خاص.
**اكتمال:** دورة كاملة: حجز → تحويل → رفع إيصال → حالة «بانتظار التحقق».

### المرحلة ٤ — لوحة التحكم: الطلبات والمحتوى
تسجيل دخول الأدمن، إدارة الحجوزات (التحقق من الإيصال، تأكيد، إلغاء)، طلبات عروض الأسعار، إدارة المسارات والأسعار والفئات والتعريفة، إدارة محتوى صفحات المسارات، الإعدادات.
**اكتمال:** كل ما يظهر في الموقع يُدار من اللوحة بدون لمس الكود، ودورة الحجز تُدار حتى «مؤكد».

### المرحلة ٥ — المتعهدون والتشغيل
سجل المتعهدين، إسناد الحجز المؤكد لمتعهد مع تسجيل تكلفته، شاشة الرحلات القادمة/المنفذة، هامش الربح يظهر لكل رحلة.
**اكتمال:** كل حجز مؤكد يُسند ويُسجَّل هامشه تلقائياً.

### المرحلة ٦ — الحسابات والمقاصة
كشف حساب شهري لكل متعهد (رحلاته − ما دُفع له)، تسجيل دفعات للمتعهدين، تقرير شهري إجمالي (إيرادات/تكاليف/هامش/تحصيلات معلقة). كله Postgres views + دوال.
**اكتمال:** تقرير شهر كامل يطلع أرقامه مطابقة للحسبة اليدوية.

### المرحلة ٧ — الإشعارات
بوت تليجرام (رسالة فورية لكل حجز/طلب/إيصال جديد)، إشعارات إيميل، مركز إشعارات داخل اللوحة.
**اكتمال:** كل حدث مهم يوصل تليجرام + إيميل خلال ثوانٍ.

### المرحلة ٨ — السيو والإطلاق
Metadata + Open Graph لكل صفحة، sitemap.xml، robots.txt، schema.org (LocalBusiness + Service)، سرعة الصفحات (Core Web Vitals)، شراء الدومين وربطه، نشر Vercel + Supabase إنتاج، فحص الإطلاق النهائي.
**اكتمال:** الموقع أونلاين على الدومين، Google Search Console متصل، أول حجز حقيقي يمر بالدورة كاملة.

> **ملحوظة حرجة من المشروع القديم:** عمر الدومين يبدأ من الشراء — **شراء الدومين لا ينتظر المرحلة ٨**، يُشترى في أقرب وقت ويُركَّب عليه صفحة بسيطة.

## ٧. ما بعد الإطلاق (Backlog — ممنوع دخوله النسخة الأولى)

- بوابة دفع إلكترونية (باي موب أو غيرها)
- اللغة الإنجليزية (صفحات حقيقية + hreflang)
- الإيجار اليومي والجولات السياحية كمنتجات محجوزة فورياً
- تغطية كل المحافظات
- قوائم أسعار المتعهدين المهيكلة واشتقاق الأرضية السعرية آلياً
- بورتال العميل الكامل (تاريخ الحجوزات، إعادة الحجز)
- تقييمات العملاء
- تكامل مع منصة الإدارة (Cars-Dashboard)

## ٨. عناصر مفتوحة (تُحسم أثناء التنفيذ — ليست قرارات معمارية)

- [ ] اسم العلامة التجارية والدومين
- [ ] قائمة فئات السيارات النهائية وأسعار التعريفة الفعلية
- [ ] قائمة الـ ١٠–٢٠ مساراً للإطلاق
- [ ] بيانات حساب التحويل (انستاباي/محفظة) التي ستظهر للعميل
- [ ] نسبة العربون والحد الأدنى + نسبة خصم التسجيل
- [ ] حسابات Google Cloud (Maps API) وSupabase وVercel والدومين
