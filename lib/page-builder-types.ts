import type { PageKind, SectionType } from "@/lib/content-types";
import type { NonTextFieldName } from "@/lib/item-fields-types";

/**
 * عقد منشئ الصفحات (المرحلة ١٣) — المرجع الأوحد. يُقرأ **قبل** أول سطر SQL
 * وقبل أول مكوّن، ومعه `docs/phase-briefs/PAGE-BUILDER.md`.
 *
 * ملفات تُقرأ معه ولا يُغني عنها: `lib/content-types.ts` (نموذج الأقسام) ·
 * `supabase/migrations/0018_i18n.sql` (طبقة الترجمة) · **D-23** و**D-24**
 * و**D-25** و**D-20** و**D-48** و**D-58**.
 *
 * ── لماذا وُجد هذا الملف ────────────────────────────────────────────────────
 *
 * المنشئ يولّد **شجرة**، وطبقة الترجمة تفهرس **مستويين**. والاصطدام لا يظهر
 * انهياراً — يظهر صمتاً، وهذا أسوأ ما فيه.
 *
 * ── ما قِيس فعلاً على القاعدة الحية (2026-08-15، داخل معاملة أُلغيت) ────────
 *
 * ١) **الشكل القائم مستويان لا أكثر، والأرقام تثبته:** ١٧ صفحة · ٩٣ قسماً ·
 *    ١٠ أنواع. ومفاتيح `content` كلها سبعة: `title`(٧٩) `body`(٤٠) `sub`(٢١)
 *    `items`(١٩ مصفوفة) `note`(١٣) `ctaLabel`(١٢) `headline`(١). **صفر** مصفوفة
 *    باسم غير `items`، و**صفر** كائن متداخل في ٩٣ قسماً. وعناصر `items` كلها
 *    كائنات مسطّحة من نصوص (`q`/`a` و`title`/`text`).
 *
 * ٢) **الفهرس يعرف صيغتين اثنتين فقط:** `i18n_corpus_rows()` تُخرج ٢٩٠ مفتاح
 *    قسم — **١٦٦** بصيغة `<sectionId>.<field>` و**١٢٤** بصيغة
 *    `<sectionId>.items.<ordinal>.<field>`. ولا صيغة ثالثة في القاعدة كلها.
 *
 * ٣) 🔴 **العمق الزائد لا ينفجر — يختفي.** قسمٌ اختباري حمل `columns:[{heading}]`
 *    و`style:{label}` أخرج **صفر** صفٍّ من الفهرس، بينما أخرج `items` أربعة.
 *    و`i18n_apply` مرّرت الاثنين كما هما. فالنتيجة ليست خطأً بل:
 *      • نصٌّ عربي يظهر على `/en` بلا أن يعلم أحد،
 *      • و`translation_progress().percent` يُحسب على **الفهرس** لا على الصفحة
 *        ⇒ العدّاد يقول ١٠٠٪ والصفحة نصفها عربي،
 *      • و`enabled_locales().published_count` يعدّ **صفوفاً** لا تغطية ⇒
 *        **D-25 يصير غير قابل للفرض**: تُعلَن اللغة في hreflang وخريطة الموقع
 *        وفيها محتوى غير مترجَم أصلاً.
 *
 * ٤) 🔴 **العنوان الترتيبي ينكسر بالحركة التي يقوم عليها المنشئ.** بُدّل عنصرا
 *    `faq` (وهو ما يفعله السحب والإفلات حرفياً) فصارت الصفحة الإنجليزية تعرض
 *    `{ q: "QUESTION-ALEF", a: "جواب باء" }` — سؤالٌ مترجَم ملصوقٌ بجواب عنصرٍ
 *    آخر، **معروضٌ على الزائر**. الطابور وسمه `stale=true`، لكن `localized_page`
 *    ترشّح على `status='published'` وحده فتخدمه. واليوم إعادة الترتيب حركةٌ
 *    نادرة خلف زرَّين؛ وفي المنشئ **هي التفاعل نفسه**.
 *
 * فالخلاصة: القيد على الشكل صحيح، لكن **الشكل القائم ليس آمناً كما هو**.
 */

// ---------------------------------------------------------------------------
// (١) 🔴 القرار المحوري — الترجمة تعنون بمسارٍ **مسطّح ثابت**، والبنية لا تُترجَم
// ---------------------------------------------------------------------------

/**
 * **القرار:** الخيار (ج) الهجين، بإضافةٍ لا تحتملها صياغته المجرّدة:
 *
 *   • كل نصٍّ قابل للترجمة يبقى على **مستويين من جذر القسم**: حقلٌ نصّي أعلى،
 *     أو حقلٌ نصّي داخل عنصرٍ من قائمة **واحدة** اسمها `items` حرفياً.
 *   • «التداخل» الذي تطلبه الخارطة يُنفَّذ **تداخلَ كتل** (‏`sections.parent_id`)
 *     لا تداخلَ حقول — أي عمقاً في **صفوف** الجدول لا في `jsonb`.
 *   • وعنوان العنصر يصير **مفتاحاً ثابتاً** (`_k`) لا ترتيباً.
 *   • وكل ما ليس نصاً يعيش تحت مفتاحٍ محجوز واحد (`style`) **لا يدخله الفهرس
 *     أبداً** — فـ«غير قابل للترجمة» يُعلَن بـ**موضعه** لا يُستنتَج من نوعه.
 *
 * **المبرر:** الصيغتان اللتان تعرفهما القاعدة تبقيان صيغتين، فلا يتضخم فضاء
 * المفاتيح ولا يفقد `translation_progress` مقاماً ثابتاً. والعمق الذي يريده
 * المالك يصل كاملاً — لكن عبر آلية **تعرفها الترجمة أصلاً**: كل كتلة ابنة قسمٌ
 * له `id` خاص، فنصوصها مفهرسة بذلك المعرّف تلقائياً وبلا سطرٍ جديد في الفهرس.
 *
 * **عاقبة النقض — إن سُمح للشجرة أن تعمق داخل `jsonb`:**
 *   • مفاتيح `translations.key` تصير مساراتٍ **يتغيّر معناها بسحبةٍ واحدة**،
 *     والفهرس الفريد `(locale, namespace, key)` يحمل عنواناً غير مستقر.
 *   • ونصفُ محتوى الصفحات المبنية لا يظهر في الطابور إطلاقاً ⇒ عودةٌ إلى
 *     العطب المقيس في (٣) أعلاه، مضروبةً في كل صفحة يبنيها المالك.
 *   • **ولا رجعة:** المفتاح الذي نُشر لا يُسحب — تصحيحُ العنونة بعد النشر يعني
 *     إسقاط ترجماتٍ راجعها إنسان.
 *
 * **ولماذا لا (أ) ولا (ب):**
 *   • (أ) «قيّد المنشئ على الشكل القائم» — القيد صحيح والشكل معطوب: لو شُحن كما
 *     هو لشُحن عطبُ إعادة الترتيب المقيس في (٤) **ميزةً**.
 *   • (ب) «وسّع الترجمة لعمقٍ حر» — أغلى الثلاثة نقضاً، وقد فصّلته الفقرة أعلاه.
 */
export const TRANSLATION_SHAPE_DECISION = "flat-path-stable-key" as const;

/** إصدار شكل `content` — يُكتب في `style._v` عند أول كتابة من المنشئ */
export const BUILDER_CONTENT_VERSION = 1;

// ---------------------------------------------------------------------------
// (٢) نموذج الصفحة — لا جدول ثالث، و**لا ترحيل بيانات**
// ---------------------------------------------------------------------------

/**
 * **القرار:** `pages` + `sections` هما النموذج، حرفياً وبلا بديل (**D-23**).
 * المرحلة ١٣ تضيف **أعمدة وجداول مجاورة** ولا تنقل صفاً واحداً:
 *   • `sections.parent_id uuid null references sections(id) on delete cascade`
 *   • `sections.block_key text null`  ← مفتاح ثابت للكتلة داخل قالبٍ مصدَّر
 *   • جدول `page_revisions` (المسودة كاملةً لقطةً) — القسم ٧
 *   • جدول `block_registry` (كتالوج الكتل في القاعدة) — القسم ٦
 *
 * **المبرر:** الضمانة المكتوبة في الخارطة («كل صفحات النظام القديم تُفتح
 * وتُحرَّر في المنشئ بلا كسر») تتحقق مجاناً حين لا يكون هناك ما يُنقَل. والقسم
 * القائم يصير كتلةً بمجرد أن يُسجَّل نوعه في الكتالوج.
 *
 * **عاقبة النقض:** نموذج بياناتٍ ثانٍ ⇒ ترحيل ١٧ صفحة و٩٣ قسماً، **وإسقاط ٢٩٠
 * مفتاح ترجمة** لأن المفاتيح مبنية على `sections.id`؛ وضمانة «بلا كسر» تسقط في
 * أول سطر.
 *
 * ⚠ **والحارس على هذا القرار مقيس لا مُعلَن:** فحصٌ في مجموعة الاختبار يقارن
 * ناتج `i18n_corpus_rows()` **قبل الهجرة وبعدها** ويشترط تطابق ٣٦١ صفاً حرفياً
 * (٢٩٠ قسم · ٥١ صفحة · ٨ مركبة · ٦ خدمة · ٦ إعدادات).
 */
export const PAGE_BUILDER_TABLES = [
  "sections", // + parent_id, block_key
  "pages", // + kind 'landing'
  "page_revisions", // جديد
  "block_registry", // جديد
] as const;

// ---------------------------------------------------------------------------
// (٣) التداخل — تداخل **كتل** لا تداخل **حقول**
// ---------------------------------------------------------------------------

/**
 * **القرار:** الكتلة التخطيطية `columns` تقبل أبناءً عبر `parent_id`، والأبناء
 * صفوفٌ في `sections` لها معرّفاتها وترتيبها. العمق **مستوى واحد** (كتلة أب
 * وأبناؤها) ولا يُسمح بحفيد.
 *
 * **المبرر:** كل نص في الصفحة يبقى على بُعد مستويين من **جذر قسمه**، وهو
 * بالضبط ما يعنونه الفهرس. والعمق الثاني كان سيضيف قدرةً تخطيطية لا يطلبها
 * أحد، ويفتح دوراتٍ في `parent_id` تحتاج حارساً بنيوياً بذاته.
 *
 * **عاقبة النقض:** جدولٌ من صفوف وأعمدة (وهو الشكل الوحيد الذي يستدعي حفيداً)
 * يصير قابلاً للبناء — وكل خلية فيه نصٌّ لا يعرف الفهرس عنوانه.
 *
 * **الثمن، مكتوباً بلا تجميل:** بناء صفٍّ من ثلاثة أعمدة نصية = **٤ صفوف** في
 * `sections` لا صفٌّ واحد، والمالك يتعلّم أن «العمود كتلة». وهذا هو الفارق
 * الحقيقي بين ما تعده الخارطة بكلمة «تداخل» وما يُشحن.
 */
export const MAX_BLOCK_DEPTH = 1;

/** حارسٌ بنيوي: `parent_id` لا يشير إلى كتلةٍ لها أبٌ (لا حفيد) ولا إلى نفسها */
export const NESTING_GUARD_TRIGGER = "sections_guard_depth" as const;

// ---------------------------------------------------------------------------
// (٤) 🔴 مفتاح العنصر `_k` — العلاج المباشر للعطب المقيس في الترويسة (٤)
// ---------------------------------------------------------------------------

/**
 * **القرار:** كل عنصر في `items` يحمل `_k`: ست خانات `[a-z0-9]` تُسَكّ عند
 * **إنشاء العنصر** ولا تتغير أبداً ولا يُعاد استعمالها. ويصير عنوان الترجمة:
 *
 *     <sectionId>.items.<_k>.<field>        ← الجديد
 *     <sectionId>.items.<ordinal>.<field>   ← القديم، **يبقى يعمل**
 *
 * `i18n_corpus_rows` تُخرج الصيغة الأولى حين يوجد `_k` والثانية حين لا يوجد،
 * و`i18n_apply` تبحث بـ`_k` أولاً ثم تسقط إلى الترتيب. فالمفاتيح الـ١٢٤ القائمة
 * لا تتغير ولا يُرحَّل صفٌّ واحد؛ والعنصر الذي يلمسه المنشئ يكتسب `_k` ويهاجر
 * مفتاحه **مرةً واحدة** بوصفه نصاً جديداً في الطابور.
 *
 * **المبرر:** العنوان يجب أن يصف **العنصر** لا **موضعه**، وإلا كان كل سحبٍ
 * إعادةَ إسنادٍ صامتة لترجماتٍ منشورة.
 *
 * **عاقبة النقض (وهي مقيسة لا متوقَّعة):** سحبةٌ واحدة تنقل ترجمة العنصر الأول
 * إلى الثاني، وتُعرَض على الزائر ممزوجةً بعربية العنصر الآخر. والوسم `stale`
 * يظهر في اللوحة فقط — **الموقع يخدم الخطأ إلى أن يفتح المالك الطابور**.
 *
 * ⚠ **وقاعدة مشتقة إلزامية:** السحب والإفلات **لا يُفعَّل** على `items` كتلةٍ
 * لا يحمل عناصرها `_k`. زرّا «أعلى/أسفل» في المحرر القديم يبقيان — لكن المنشئ
 * لا يعرض مقبض السحب إلا بعد سكّ المفاتيح.
 */
export const ITEM_KEY_FIELD = "_k" as const;
export const ITEM_KEY_PATTERN = /^[a-z0-9]{6}$/;
export type ItemKey = string;

/** المفتاح المحجوز للتنسيق — القسم ٥ */
export const STYLE_FIELD = "style" as const;
export const ITEMS_FIELD = "items" as const;

/** مفاتيح لا يجوز أن يحملها حقلٌ نصّي قابل للترجمة، ولا أن يخترعها كاتب كتلة */
export const RESERVED_CONTENT_KEYS = [ITEM_KEY_FIELD, STYLE_FIELD] as const;

/** بُناة العناوين — **مصدر واحد** تشترك فيه TypeScript وSQL بنصّها */
export function sectionFieldKey(sectionId: string, field: string): string {
  return `${sectionId}.${field}`;
}
export function sectionItemKey(sectionId: string, itemKey: ItemKey, field: string): string {
  return `${sectionId}.${ITEMS_FIELD}.${itemKey}.${field}`;
}
/** الصيغة القديمة — تبقى مقروءةً ولا تُكتب في عنصرٍ جديد */
export function legacySectionItemKey(sectionId: string, ordinal: number, field: string): string {
  return `${sectionId}.${ITEMS_FIELD}.${ordinal}.${field}`;
}

// ---------------------------------------------------------------------------
// (٥) `style` — منطقةٌ لا تدخلها الترجمة، ولا يدخلها CSS حرّ
// ---------------------------------------------------------------------------

/**
 * **القرار:** كل ما ليس نصاً للزائر يعيش تحت `content.style` وحده، والفهرس
 * يتجاوز هذا المفتاح **بالاسم**. وقيم `style` **رموزٌ من الثيم لا ألوان خام**.
 *
 * **المبرر (شقّان):**
 *  (أ) **للترجمة:** «غير قابل للترجمة» يجب أن يكون قراراً معلَناً بموضعه، لا
 *      استنتاجاً من نوع القيمة. فلو أضاف كاتب كتلةٍ حقلاً نصياً للتنسيق
 *      (`align: "start"`) لدخل الفهرسَ ولطُلبت ترجمته وظهر في الطابور نصاً
 *      عربياً بلا معنى — ولترجمه مزوّد آلي فعلاً.
 *  (ب) **للعلامة (D-04 · D-01):** لونٌ خام مكتوبٌ في صفٍّ يكسر النسخة الثانية
 *      صامتاً، لأن النسخة الثانية تُطلق بتغيير **بيانات** لا كود — وبيانات
 *      المحتوى تُصدَّر معها قالباً.
 *
 * **عاقبة النقض:** لونٌ محفور في محتوى صفحة ⇒ الـ Whitelabel يعطي علامةً ثانية
 * لونَ الأولى في كل صفحة مبنية؛ ونصُّ تنسيقٍ في الفهرس ⇒ طابور مراجعةٍ يُعلَّم
 * قارئه أن يتجاوزه (وهو النمط نفسه الذي وصفته القاعدة الذهبية ١٣).
 *
 * ⚠ **و«إضافة CSS» في الرؤية مؤجَّلة لا محذوفة (D-39)** — سببها في §١٢.
 */
export const THEME_COLOR_TOKENS = [
  "default", // بلا تجاوز — يرث القسم لونه من الثيم
  "primary",
  "secondary",
  "accent",
  "muted",
  "card",
  "brand-accent",
] as const;
export type ThemeColorToken = (typeof THEME_COLOR_TOKENS)[number];

export const SPACING_TOKENS = ["compact", "default", "roomy"] as const;
export type SpacingToken = (typeof SPACING_TOKENS)[number];

/**
 * 🆕 **م‑١٠ — نبرة التنبيه البارز.** رمزان مغلقان، **ومكانهما `style` بعينه**:
 * النبرة ليست نصّاً للزائر (فلا تدخل الفهرس)، وليست لوناً خاماً (فلا تكسر
 * العلامة الثانية) — وهو تعريف §٥ حرفاً.
 *
 * **ولماذا اثنان لا خمسة؟** `components/ui/card.tsx` يعرف خمس نبرات
 * (`success`/`warning`/`danger`/`info`)، ومفرداتُ الصفحة العامة أضيق من
 * مفردات اللوحة بقصد: «نجاح» حالةُ عمليةٍ يراها مشغّل، و«خطر» تكرارٌ لمعنى
 * «تحذير» على صفحة سياسات. وثلاثة رموزٍ متقاربة على شاشةٍ واحدة تعني أن أياً
 * منها لا يقول شيئاً — وهي نفس علّة «كل البطاقات ملوّنة» المكتوبة هناك.
 *
 * 🔒 **والنبرتان تُصيَّران عبر `Card` نفسه لا بجانبه** (القاعدة الذهبية ١٢):
 * `--tone-info`/`--tone-warning` معرَّفان في `app/globals.css` في الكتل
 * الثلاث (فاتح · داكن · قسمٌ رمليٌّ داخل داكن)، فالتنبيه يتبع الأرضية وحدَه
 * ويوم يأتي مبدّل م‑٩ لا يحتاج سطراً — **وصفر لونٍ مكتوب في هذا المكوّن**.
 */
export const CALLOUT_TONE_TOKENS = ["info", "warning"] as const;
export type CalloutToneToken = (typeof CALLOUT_TONE_TOKENS)[number];

/**
 * 🆕 **ن‑٤ — إيقاع أثر الكتابة في البطل.** أربعة مقابض، **ومكانها `style`
 * بعينه** لا حقولاً عليا في `content`، والسبب قابلٌ للقياس لا ذوقي:
 *
 * فهرس الترجمة **يقرأ كل مفتاحٍ قيمتُه نصّ** ولا يقرأ `block_registry` أصلاً
 * (المقيس في `0064` ثم المكتوب في `lib/item-fields-types.ts` §١). فحقلٌ أعلى
 * اسمه `typingSpeed` قيمته «normal» كان **يدخل طابور المترجم** صفّاً حيّاً،
 * ومترجمٌ مجتهد «يعرّبه» فيسقط الرمز إلى الافتراضي بلا خطأ ولا سجل. أما
 * `style` فمفتاحٌ محجوزٌ بالاسم في `i18n_reserved_content_key` — لا يدخل
 * الفهرس ولا يُستبدل في `i18n_apply`، **في كل كتلة قائمة أو قادمة**.
 *
 * والطريق الثالث (‏`non_text_fields`) مرفوض بثمنه: `NON_TEXT_FIELD_NAMES`
 * قائمةٌ **عالمية** يحرسها `block_registry_check` بنسخةٍ محفورة في جسمها،
 * فإضافة أربعة أسماء إليها تعني هجرةً تعيد كتابة حارسٍ عام ودالةَ فهرسة —
 * لأجل مقابض حركةٍ في كتلةٍ واحدة. **الكلفة لا تشتري شيئاً لا يشتريه `style`.**
 *
 * ولماذا رموزٌ لا أرقام مللي ثانية؟ نفس مذهب `SPACING_TOKENS` و`tone`: حقلُ
 * رقمٍ حرّ يقبل `0` فيتجمّد الأثر، ويقبل `5000` فيقف السطر نصف دقيقة — ولا
 * شيء يقول لمن كتبه إن ما كتبه معطوب. والرموز الثلاثة تغطي المدى المعقول كله.
 */
export const TYPING_SPEED_TOKENS = ["slow", "normal", "fast"] as const;
export type TypingSpeedToken = (typeof TYPING_SPEED_TOKENS)[number];

/** مهلة ثبات الجملة المكتملة قبل محوها */
export const TYPING_HOLD_TOKENS = ["short", "normal", "long"] as const;
export type TypingHoldToken = (typeof TYPING_HOLD_TOKENS)[number];

/**
 * 🆕 **شكل كتلة «المزايا»** — بأمر بدر بعد أن فتح الموقع على هاتفه:
 * «الحجز في ثلاث خطوات… أري أنها مختلفة تماماً في الموقع حالياً عن الموجود
 * في Tours-02».
 *
 * ── ولماذا رمزٌ في `style` لا كتلةٌ جديدة ولا حقلٌ في `block_registry` ──────
 *
 * الرئيسية تحمل **كتلتَي `features`**: «الحجز في ثلاث خطوات» و«ست ضمانات
 * مكتوبة». وهما نفس البنية حرفاً (‏`title` + `items[{title,text,icon}]`)
 * ويختلفان في **شكل العرض وحده**: الأولى مسارٌ مرقَّم بخطٍّ يربط خطواته،
 * والثانية شبكةُ بطاقات. فلو حُوِّلت العارضة إلى مسارٍ مرقَّم لصارت الضمانات
 * الست «خطوات» يُقرأ ترتيبها إجراءً — وهو خطأُ معنى لا خطأ ذوق.
 *
 * فالمطلوب **تمييزٌ بين صفَّين من النوع نفسه**، وهذا تعريف `style` حرفاً:
 *   • ليس نصّاً للزائر ⇒ لا يدخل فهرس الترجمة (‏`i18n_reserved_content_key`)
 *   • ليس لوناً خاماً ⇒ لا يكسر العلامة الثانية
 *   • رمزٌ من قائمة مغلقة ⇒ القيمة المخرَّبة تسقط إلى الافتراضي لا إلى عطل
 *
 * وكتلةٌ جديدة (`steps`) كانت تكلّف: صفّاً في `block_registry`، وهجرةً، وسطراً
 * في `page_builder_tests.sql`، **وترحيلَ الصفّ الحيّ من نوعٍ إلى نوع** — أي
 * `sections.id` جديداً ⇒ **فقدان كل ترجمات الكتلة** (المكتوب في §٧ أعلاه).
 * والثمن كله لا يشتري حقلاً واحداً لا يحمله `features` اليوم.
 *
 * 🔒 **والترقيم موضعيّ لا حقل**: «١ ٢ ٣» هي `index + 1` بأرقام لغة الزائر —
 * فإعادةُ ترتيب عنصرٍ من المنشئ تعيد ترقيمه وحدها، ولا يبقى رقمٌ مكتوبٌ في
 * نصٍّ يناقض موضعه.
 *
 * ── 🆕 والرمزان الثالث والرابع (2026-08-17) ────────────────────────────────
 *
 * أضيفا لأجل «ست ضمانات مكتوبة» — وهي **الصفّ الثالث** من هذا النوع، وشكواه
 * ارتفاعُه على الجوال (١٦٥٧ بكسل عند ٣٧٥).
 *
 * | الرمز | الشكل | متى يكسب |
 * |---|---|---|
 * | `compact` | صفوفٌ بلا بطاقة: الأيقونة والعنوان على سطرٍ واحد والنصّ تحته | حين تُقرأ القائمة **كاملةً** — وعدٌ لا خيار |
 * | `rail` | سكةٌ أفقية بنفس آلية المسارات والأسطول والخدمات | حين يُقارَن العناصر ويُنتقى منها واحد |
 *
 * 🔴 **والقسمة بينهما ليست ذوقاً بل غرضَ المحتوى، والتصميم يقولها بنفسه:**
 * `#fleet` و`#routes` و`#svcx` **سكك** (`.rail`) لأن العميل يختار منها واحداً؛
 * أما `#promise` — الضمانات الست — فقائمةٌ رأسية عادية (`.gua__list`) **ولم
 * تُجعل سكةً**. وضمانةٌ مخبوءة خلف سحبةٍ لا تُقرأ، والقائمة كلها هي الرسالة.
 */
export const FEATURES_LAYOUT_TOKENS = ["cards", "steps", "compact", "rail"] as const;
export type FeaturesLayoutToken = (typeof FEATURES_LAYOUT_TOKENS)[number];

/**
 * 🆕 **شكل كتلة «لماذا نحن»** — بأمر بدر عن «ما تحصل عليه في كل حجز»:
 * «أسلوب العرض الحالي يستهلك مساحة كبيرة نسبياً على الموبايل… يمكن عرض الأمر
 * بشكل مختلف مع استخدام رسومات أو مؤثرات معينة».
 *
 * والرمزان **يُبنيان معاً ويُعرضان عليه ليختار** — لا يُختار عنه:
 *
 * | الرمز | ما هو | متى يكسب |
 * |---|---|---|
 * | `dense` | صفوفٌ مضغوطة: الأيقونة والنصّ على سطرٍ واحد، بلا بطاقةٍ لكل بند | القسم **يُقرأ ليُقرَّر** لا ليُعجَب؛ والشاشة صغيرة والصبر أقصر |
 * | `expressive` | وزنٌ بصريّ أعلى: أرقامٌ وحركةٌ وتدرّجٌ عند دخول الشاشة | حين تكون الرسالة «نحن محترفون» لا «إليك ما تحصل عليه» |
 *
 * والافتراضي `dense` لأنه الجواب المباشر على الشكوى المقيسة (الارتفاع)، **وتبديلُه
 * قائمةٌ واحدة في المنشئ** لا نشرةُ كود.
 */
export const WHY_US_LAYOUT_TOKENS = ["dense", "expressive"] as const;
export type WhyUsLayoutToken = (typeof WHY_US_LAYOUT_TOKENS)[number];

/**
 * 🆕 **مقابض شريط الماركات (ن‑٩) — خمسةٌ كلها في `style`، ولا واحد منها نصّ.**
 *
 * والمبرر هو مبرر ن‑٤ حرفاً ولا يُعاد شرحه: الفهرس يقرأ **كل مفتاحٍ قيمتُه
 * نصّ** ولا يقرأ `block_registry`، فحقلٌ أعلى اسمه `marqueeSpeed` قيمته
 * «normal» كان يدخل طابور المترجم صفّاً حيّاً — ومترجمٌ مجتهد «يعرّبه» فيسقط
 * الرمز إلى الافتراضي بلا خطأ ولا سجل. و`style` محجوزٌ بالاسم في
 * `i18n_reserved_content_key`، فلا يدخل الفهرس ولا يستبدله `i18n_apply`.
 *
 * ⚠ **وشرطٌ يحكم القوائم الخمس كلها: القيمة المجهولة تنحدر إلى الافتراضي ولا
 * تُسقط صفحةً عامة.** التطهير في `readBlockStyle` لا في المحرر وحده — لأن
 * الإدراج المباشر من PostgREST أو من محرر SQL لا يمرّ على أي شاشة.
 */
export const MARQUEE_SPEED_TOKENS = ["slow", "normal", "fast"] as const;
export type MarqueeSpeedToken = (typeof MARQUEE_SPEED_TOKENS)[number];

/**
 * 🔴 **اتجاهٌ نسبيّ لا مطلق — «مع القراءة» و«عكسها»، لا «يمين» و«يسار».**
 *
 * القرار (٤) في `components/sections/logo-strip.tsx` مكتوبٌ منذ م‑٢: الشريط
 * في RTL يزحف يميناً وفي LTR يساراً، لأن `transform` لا يعرف الاتجاه. فمقبضٌ
 * يقول «يمين» كان سينتج شريطاً يزحف إلى فراغٍ في إحدى اللغتين — والصفحة
 * الإنجليزية موجودة (D-24)، فالعطب ليس فرضياً.
 */
export const MARQUEE_DIRECTION_TOKENS = ["auto", "reverse"] as const;
export type MarqueeDirectionToken = (typeof MARQUEE_DIRECTION_TOKENS)[number];

/**
 * أثر الصورة على الشعار. **وثلاثةٌ لا أربعة، والرابع مرفوضٌ بقياس:**
 *
 * | الرمز | ما يفعله |
 * |---|---|
 * | `mono` | أحادية اللون، **وتتلوّن عند التحويم** — وهو سلوك اليوم حرفاً |
 * | `monoStill` | أحادية اللون دائماً، بلا تلوّن |
 * | `color` | بألوانها الأصلية **على الأرضية الفاتحة وحدها** |
 *
 * 🔴 **ولماذا يبقى `color` أحادياً على الداكن — وهو ليس نقصَ تنفيذ:** الشعارات
 * العشرة ملفات SVG بألوانها الأصلية، **وأكثرها أسود** (تويوتا · هوندا · كيا ·
 * هيونداي). ولمّا لبس الموقع القشرة الداكنة صار الشريط فراغاً مقيساً: عنوانٌ
 * ثم مسافة فارغة ثم ملاحظة. فرمزٌ يعِد بالألوان على الداكن يعِد **باختفاء
 * الشعارات السوداء** — وهو أسوأ ما يفعله مقبضٌ في لوحة تحكّم: يُشحن معطوباً
 * خلف خيار، ولا شيء يقول لمن اختاره إن ما اختاره لا يعمل (النمط ٣ في
 * `LESSONS.md`). والعلاج الوحيد الآخر لوحةٌ فاتحة خلف كل شعار — وهي تغييرُ
 * تصميمٍ لا مقبضُ تنسيق.
 */
export const LOGO_EFFECT_TOKENS = ["mono", "monoStill", "color"] as const;
export type LogoEffectToken = (typeof LOGO_EFFECT_TOKENS)[number];

/**
 * فعل النقر على الشعار. **رمزٌ يفتح الباب، و`href` العنصر يقول إلى أين** —
 * ولا يُدمجان في حقلٍ واحد:
 *
 *   `none` (الافتراضي) الشعار صورةٌ لا رابط، ولو حمل العنصر `href`.
 *   `item`             الشعار الذي يحمل `href` **داخلياً** يصير رابطاً.
 *
 * 🔒 **ولماذا الافتراضي «لا شيء»؟** لأن الشعار ادّعاءُ طرازٍ متاح لا شراكة،
 * ورابطٌ عليه يقرأه الزائر إحالةً إلى الشركة المصنِّعة. وفتحُه قرارُ مالكٍ
 * يُتخذ بقصد، لا سلوكٌ يرثه من ترقية.
 *
 * ⚠ **والوجهة تمرّ من `internalPath` كما في `route-rail`**: ما لا يبدأ بـ`/`
 * يُصيَّر صورةً بلا رابط. وبدون ذلك كان حقلٌ في محتوى صفحةٍ عامة يفتح تحويلاً
 * إلى نطاقٍ آخر من قلب الرئيسية.
 */
export const LOGO_LINK_TOKENS = ["none", "item"] as const;
export type LogoLinkToken = (typeof LOGO_LINK_TOKENS)[number];

/**
 * شكل `content.style`. كل حقوله اختيارية، **والغياب يعني «الافتراضي»** لا
 * «صفر» — نفس تمييز القاعدة الذهبية ١٥.
 */
export type BlockStyle = {
  /** إصدار شكل المحتوى — يسمح بترحيلٍ لاحق بلا تخمين */
  _v?: number;
  background?: ThemeColorToken;
  spacing?: SpacingToken;
  /** إخفاء الكتلة على الجوال — تخطيط لا محتوى */
  hideOnMobile?: boolean;
  /**
   * 🆕 م‑١٠ — نبرة `callout` وحدها (‏`styleKeys` هي الحدّ: ما ليس فيها لا
   * يُعرض في المحرر). والغياب يعني `info` لا «بلا نبرة».
   *
   * ⚠ **وهي المقبض الوحيد في `BlockStyle` الذي يصل إلى مكوّن الكتلة نفسه** —
   * لأن `sanitizeContent` تُسقط `style` قبل العارضة بقصد (فلا يُصيَّر رمزُ
   * تنسيقٍ نصّاً بالخطأ). فيُمرَّر **مطهَّراً** عبر `SectionProps.style`، ولا
   * يقرأ أي مكوّن `content.style` بنفسه.
   */
  tone?: CalloutToneToken;
  /**
   * 🆕 ن‑٤ — مقابض أثر الكتابة في البطل. تسلك مسلك `tone` حرفاً: تصف **داخل**
   * الكتلة لا غلافها، فلا تُخرج صنفاً في `blockStyleClass`، وتصل المكوّن
   * مطهَّرةً عبر `SectionProps.style` وحده.
   *
   * والغياب يعني الافتراضي: `normal` للسرعات والمهلة، و**التوقف** للتكرار —
   * وهو قرار بدر صراحةً: حركةٌ دائمة تشتّت عميلاً يملأ نموذج الحجز.
   */
  typingSpeed?: TypingSpeedToken;
  typingHold?: TypingHoldToken;
  typingErase?: TypingSpeedToken;
  /** يتكرر بلا توقف؟ الغياب = يقف عند آخر جملة (الافتراضي) */
  typingLoop?: boolean;
  /**
   * 🆕 شكل «المزايا» و«لماذا نحن». تسلكان مسلك `tone` حرفاً: تصفان **داخل**
   * الكتلة لا غلافها، فلا تُخرجان صنفاً في `blockStyleClass`، وتصلان المكوّن
   * مطهَّرتين عبر `SectionProps.style` وحده.
   */
  featuresLayout?: FeaturesLayoutToken;
  whyUsLayout?: WhyUsLayoutToken;
  /**
   * 🆕 ن‑٩ — مقابض شريط الماركات. تسلك مسلك `tone` حرفاً: تصف **داخل** الكتلة
   * لا غلافها، فلا تُخرج صنفاً في `blockStyleClass`، وتصل المكوّن مطهَّرةً عبر
   * `SectionProps.style` وحده.
   */
  marqueeSpeed?: MarqueeSpeedToken;
  marqueeDirection?: MarqueeDirectionToken;
  /**
   * يقف الشريط عند التحويم؟ **الغياب يعني «نعم»** — وهو الاستثناء الوحيد في
   * هذا الشكل الذي لا يعني فيه الغياب `false`.
   *
   * 🔒 **ولا يُقلب إلى `marqueeNoPause`** رغم أن ذلك كان يوحّد النمط: التوقّف
   * عند التحويم شرطُ استعمالٍ لا زينة — من يقرأ اسم ماركةٍ يمرّ يحتاج أن يقف
   * الشريط له. فالافتراضي الآمن هو الوقوف، والمقبض يُطفئه بقصدٍ صريح
   * (`false` مكتوبةً في الصفّ) لا بغيابٍ يورَث من صفٍّ قديم.
   *
   * ⚠ وتوقّفُ **التركيز** بلوحة المفاتيح لا يُطفئه هذا المقبض ولا غيره: من
   * يتنقّل بالـTab لا يستطيع ملاحقة رابطٍ يهرب منه، وهو شرط إتاحة لا خيار.
   */
  marqueePause?: boolean;
  logoEffect?: LogoEffectToken;
  logoLink?: LogoLinkToken;
};

// ---------------------------------------------------------------------------
// (٦) سجل الكتل — كتالوجٌ في القاعدة، وحارسٌ يجعل الشكل غير القانوني غير قابل للكتابة
// ---------------------------------------------------------------------------

/** دور الكتلة في الصفحة */
export type BlockRole =
  | "layout" // تحمل أبناءً ولا نصَّ لها (`columns`)
  | "content" // نصّها من `content` (‏`rich-text`, `faq`, …)
  | "system"; // بياناتها من الإعدادات أو `site-config` والنصّ تجاوزٌ اختياري

/** أين يُسمح بوضع الكتلة وكم مرة */
export type BlockPlacement =
  | "any" // في أي صفحة وبأي عدد
  | "once-per-page" // مرة واحدة لكل صفحة
  | "home-only"; // في صفحة `kind='home'` وحدها، ومرة واحدة

/**
 * تعريف الكتلة الواحدة. **هذا هو العقد الذي يقرؤه الحارس البنيوي**:
 * هجرة `0058` تمرّ على كل صفٍّ في `block_registry` وترفع استثناءً إن أعلنت
 * كتلةٌ حقلاً لا تستطيع قواعد العنونة في §٤ أن تعنونه — فالشكل غير القانوني
 * **لا يُكتب** بدل أن يُكتشف لاحقاً بكاشفٍ نصّي (القاعدة الذهبية ١٩).
 */
export type BlockDef = {
  type: BuilderBlockType;
  role: BlockRole;
  placement: BlockPlacement;
  /** يقبل أبناءً؟ `layout` وحدها */
  acceptsChildren: boolean;
  maxChildren: number | null;
  /** الحقول النصية العليا القابلة للترجمة */
  textFields: readonly string[];
  /** حقول العنصر داخل `items` — `null` = لا قائمة في هذه الكتلة */
  itemFields: readonly string[] | null;
  /**
   * 🆕 **م‑٧** — حقولٌ عليا **غير نصّية** (مسارٌ أو اسم أيقونة): لا تدخل فهرس
   * الترجمة ولا يستبدلها `i18n_apply`. `null` = لا شيء (لا `{}`).
   *
   * وظيفتها **الإعلان والتحقق لا الترشيح**: الترشيح بالاسم في
   * `public.i18n_non_text_field` (‏`lib/item-fields-types.ts` §١)، وهذه تقول
   * للمحرر أي عنصر واجهة يعرض، وتجعل `requiredFields` مقروءةً على اتحاد
   * القوائم الأربع، وتجعل الكتلة تصف نفسها كاملةً في صفٍّ واحد.
   */
  nonTextFields: readonly NonTextFieldName[] | null;
  /** نفسها داخل عنصر `items` — يلزمها `itemFields`، و`src` منها يلزمه `alt` */
  nonTextItemFields: readonly NonTextFieldName[] | null;
  /**
   * ما بدونه لا تُصيَّر الكتلة إطلاقاً (تُرجع `null`).
   * حقلٌ في `requiredFields` ولا في `textFields`/`nonTextFields` = خطأ تسجيل.
   */
  requiredFields: readonly string[];
  /** مقابض التنسيق المتاحة لهذه الكتلة — ما ليس هنا لا يُعرض في المحرر */
  styleKeys: readonly (keyof BlockStyle)[];
};

/**
 * أنواع الكتل = أنواع الأقسام العشرة القائمة + كتلتان جديدتان.
 * ⚠ `sections.type` **بلا قيد `check`** في القاعدة (مقيس) — فالتسجيل هنا وفي
 * `block_registry` هو الحدّ الوحيد، و`render.tsx` تتجاهل المجهول بأمان (مقيس).
 */
export type BuilderBlockType = SectionType | "columns" | "image";

// ---------------------------------------------------------------------------
// (٧) المسودة والنشر — لقطةُ مراجعة، **والنشر فرقٌ بالمعرّف لا حذفٌ وإدراج**
// ---------------------------------------------------------------------------

/**
 * **القرار:** المنشئ يكتب في `page_revisions` (لقطة `jsonb` للصفحة كاملةً
 * بأقسامها)، ولا يلمس `sections` إلا لحظة النشر. والنشر **فرقٌ يُطابَق
 * بـ`sections.id`**: يُحدَّث الموجود، ويُدرَج الجديد، ويُحذف المرفوع من اللقطة —
 * كلٌّ في **معاملةٍ واحدة** (‏**D-48**: كل نداء PostgREST معاملة واحدة، فالنشر
 * دالةٌ واحدة `publish_page_revision(p_page uuid, p_revision uuid)` لا سلسلة
 * نداءات من الواجهة).
 *
 * **المبرر:**
 *  • القراءة العامة و`i18n_corpus_rows` تقرآن `sections` وحدها ⇒ **المسودة لا
 *    تصل الفهرس ولا الطابور ولا `enabled_locales`** ⇒ D-25 يبقى مفروضاً بلا
 *    سطرٍ إضافي، ولا تتضخم الترجمة بنصوصٍ لم تُنشر.
 *  • واللقطة تعطي المعاينة كائناً واحداً يُعنوَن، وتعطي «احفظ كقالب» معناها
 *    الحرفي: **القالب لقطة**.
 *
 * 🔴 **عاقبة النقض — والأخطر في هذه المرحلة كلها:** نشرٌ بـ«احذف أقسام الصفحة
 * ثم أدرجها من اللقطة» يبدو أبسط ويعمل تماماً في أول تجربة — **ويُبيد كل ترجمات
 * الصفحة في كل نشرة**، لأن كل مفاتيح `namespace='section'` مبنية على
 * `sections.id`. صفحةٌ فيها ٢٠ مفتاحاً تفقدها كلها بضغطة «نشر»، ولا يظهر ذلك
 * في أي شاشة عربية.
 *
 * ⚠ **ولا يُلمس `created_at`/`updated_at` بغير حاجة:** `pages_touch_updated_at`
 * يغذّي `lastModified` في خريطة الموقع؛ ونشرةٌ لا تغيّر شيئاً يجب ألا تعلن
 * تعديلاً (نفس مبرر الختم المحفور في `lib/content.ts`).
 */
export type RevisionStatus = "draft" | "published" | "archived";

export type PageRevisionRow = {
  id: string;
  page_id: string;
  status: RevisionStatus;
  /** اللقطة: `{ page: {...}, sections: [...] }` بشكل `PageWithSections` */
  snapshot: Record<string, unknown>;
  /** من أنشأها — من `profiles` */
  created_by: string | null;
  created_at: string;
  published_at: string | null;
};

/** نتيجة النشر — أعداد لا جُمل، فتُقرأ في الإيصال وفي السجل معاً */
export type PublishResult = {
  updated: number;
  inserted: number;
  deleted: number;
  /** معرّفات الأقسام التي بقيت — بها تُقاس سلامة الترجمة بعد النشر */
  keptSectionIds: string[];
};

/**
 * **المعاينة:** مسارٌ **تحت `/admin`** لا رابطٌ عام بتوكن.
 *
 * **المبرر:** `/admin` محجوب في `robots.txt`، ومحروس طبقتين (`proxy.ts` +
 * `app/admin/layout.tsx`)، وبلا وسم قياس واحد (**D-44**). أما رابط معاينة عام
 * — ولو بتوكن — فصفحةٌ غير منشورة قابلة للزحف: يكفي أن يُلصق في محادثة أو
 * يُفتح على متصفحٍ يُزامن سجلّه. والمشروع أمسك سلفاً تناقض «امنع الفهرسة» بين
 * `robots` والخريطة؛ هذا وجهه الآخر.
 *
 * ⚠ **وشرطٌ على الشاشة:** صفحة المعاينة تستعمل **حارس** `/admin` ولا تستعمل
 * **قشرته** — تُصيَّر بعرضٍ كامل بلا شريط جانبي، وإلا صارت المعاينة تكذب على
 * من ينظر إليها.
 */
export const PREVIEW_PATH = "/admin/content/[id]/preview" as const;

// ---------------------------------------------------------------------------
// (٨) المسار والـ slug
// ---------------------------------------------------------------------------

/**
 * **القرار:** نوع صفحة رابع `landing` يُضاف إلى القيد
 * `pages_kind_check` (‏**القيد قائم ومقيس**: `home|service|corridor|static`)،
 * ويُصيَّر من `app/[slug]/page.tsx` نفسه على `/{slug}` — **بلا بادئة لغة**
 * (‏**D-24**) وبلا ملف مسار جديد.
 *
 * **المبرر:** `/{slug}` هو الشكل الذي يريده المالك لصفحةٍ يبنيها، و`app/[slug]`
 * يملكه أصلاً. ونوعٌ مستقل — لا إعادة استعمال `static` — لأن `PRIORITY` في
 * `app/sitemap.ts` و`pagePublicPath` وقائمة `/admin/content` كلها تفرّق بين
 * «سياسة الخصوصية» و«صفحة هبوط تسويقية».
 *
 * ⚠ **وفائدة مجانية مقصودة:** `pagePublicPath` في `lib/seo/site-paths.ts`
 * `switch` على `PageKind` **بلا `default`** — فإضافة النوع تجعل نسيانَ
 * التعامل معه **خطأ بناء** لا نقصاً صامتاً.
 *
 * 🔴 **والتصادم موجودٌ اليوم ولا يحرسه شيء:** `createPage` يقبل أي `slug`
 * يطابق النمط، بينما `app/[slug]/page.tsx` يرفض بـ`notFound()` كل slug في
 * `RESERVED_SLUGS`. أي أن المالك **يستطيع الآن** إنشاء صفحة `about` أو `book`
 * ونشرها ورؤيتها «منشورة» في اللوحة — وهي ٤٠٤ للأبد. Next يقدّم المقطع الثابت
 * دائماً، **فالملف يفوز ولا مجال لغير ذلك**؛ والحلّ **رفضٌ عند الحفظ** لا تحذير.
 *
 * **القرار الإجرائي:** الرفض يقع في **القاعدة** (مُشغّل على `pages`) لا في
 * الـ Server Action وحدها — لأن الإدراج المباشر عبر PostgREST أو محرر SQL
 * يتخطى الواجهة (نفس مبرر `bookings_guard_return_leg` في `0032`).
 *
 * **عاقبة النقض:** صفحةٌ تبدو حيّة في اللوحة وميتة على الويب، ولا سطر في أي
 * سجل يقول ذلك — وهو بالضبط النمط ٣ في `LESSONS.md`.
 */
export const BUILDER_PAGE_KIND: PageKind | "landing" = "landing";

/**
 * مصدر المحجوزات **واحد لا يُنسخ**: `lib/seo/site-paths.ts`
 * (‏`APP_OWNED_PATHS` · `RESERVED_PATH_PREFIXES` · `RESERVED_EXACT_FILES`)
 * و`RESERVED_SLUGS` في `app/[slug]/page.tsx`. الهجرة تبذر الجدول
 * `reserved_slugs` من هذه القائمة **نصاً**، وفحصٌ في `page_builder_tests.sql`
 * يقارن الجدول بالملف ويفشل عند الانحراف.
 */
export const SLUG_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;

/** رموز رفض الـ slug — رمزٌ لكل سبب، لا رسالة واحدة عامة */
export const SLUG_REJECT_CODES = [
  "slug-format", // لا يطابق النمط
  "slug-reserved", // يملكه ملف في `app/` (`about`, `book`, `track` …)
  "slug-prefix", // يقع تحت بادئة محجوزة (`services/…`, `booking/…`)
  "slug-taken", // صفٌّ آخر في `pages` يحمله
  "slug-redirect", // مسارٌ يخطفه `redirects` — الصفحة لن تُرى أبداً
] as const;
export type SlugRejectCode = (typeof SLUG_REJECT_CODES)[number];

// ---------------------------------------------------------------------------
// (٩) من يبني — `is_admin()` وحدها
// ---------------------------------------------------------------------------

/**
 * **القرار:** حقّ البناء والنشر = `public.is_admin()`، أي `profiles.role='admin'`
 * حصراً (‏`0001_core.sql:81`). ولا دالة جديدة ولا حارس جديد.
 *
 * **المبرر (‏**D-20** حرفياً):** `authenticated` يشمل **كل متعهد** وكل عميل
 * مسجَّل منذ ١٢ب. والسياسات القائمة على `pages`/`sections` كلها `is_admin()`
 * للكتابة سلفاً، فالمنشئ لا يوسّع سطحاً — يستعمل القائم.
 *
 * ⚠ **والفخ المقيس هنا ليس في الكتابة بل في الشاشة:** `proxy.ts` يُدخل
 * `admin` **و`ops`** إلى `/admin`، بينما كل سياسة كتابة تشترط `is_admin()`.
 * فدور `ops` يفتح المنشئ اليوم، ويسحب، ويفلت، ويضغط «حفظ» — ويصطدم بفخ
 * Supabase الشهير: **صفر صفوف بلا خطأ** ⇒ `error=save` بلا سبب مفهوم، وعملُه
 * ضائع. **فالمنشئ يقرأ الدور ويُصيَّر للقراءة فقط لـ`ops`** — لا مقبض سحب ولا
 * زر نشر — بدل أن يسمح بعملٍ يُرفض عند الحفظ.
 *
 * **عاقبة النقض (لو فُتح لـ`authenticated`):** كل متعهد يحرّر صفحات الموقع
 * العام — وهو أوسع سطحٍ يمكن فتحه بخطأٍ واحد في هذا المشروع.
 *
 * 📌 **وتوسيع الحقّ إلى `ops` قرارُ مالكٍ لا قرارُ جلسة** — مفتوحٌ سلفاً في
 * `OPEN_TASKS` ج، ومكرَّرٌ هنا سؤالاً في `docs/phase-briefs/PAGE-BUILDER.md`.
 */
export const BUILDER_ROLE_CHECK = "public.is_admin()" as const;
export type BuilderAccess = "edit" | "read-only" | "denied";

// ---------------------------------------------------------------------------
// (١٠) كتالوج الكتل — وماذا تفعل الكتلة حين ينقص حقلها
// ---------------------------------------------------------------------------

/**
 * **القاعدة الحاكمة، بلا استثناء:** كتلةٌ ينقصها حقلٌ إلزامي **تُصيَّر `null`**.
 * لا نصَّ نائب، ولا صندوق «اضبطني»، ولا انهيار.
 *
 * **وهي ليست نظرية:** ثلاث كتل تفعلها اليوم بالفعل (`rich-text` و`faq` و
 * `features` ترجع `null` عند الفراغ — مقروءةً من الملفات)، **وواحدة تخالفها**:
 *
 * 🔴 `page-hero` تُصيَّر `{content.title}` **بلا شرط**، والمحتوى الابتدائي
 *    لقسمٍ جديد `{ title: "" }` (‏`app/admin/content/[id]/actions.ts`).
 *    فسحبُ ترويسة صفحة ونشرُها اليوم = **`<h1>` فارغ على صفحة عامة** — وهو عيب
 *    سيو لا يراه أحد بالعين. **إصلاحه بندٌ في المرحلة ١٣ لا تحسينٌ اختياري.**
 *
 * وثلاث قيود موضعٍ خرجت من قراءة العارضات نفسها:
 *  • `hero` **للرئيسية وحدها**: هي الكتلة الوحيدة التي تركّب ويدجت الحجز
 *    (`BookingWidget`)، ونسختان منها = ويدجتان على صفحة واحدة.
 *  • `services-grid` · `fleet` · `why-us` · `contact` **مرة واحدة لكل صفحة**:
 *    بياناتها من `site-config`/الإعدادات لا من `content`، فتكرارها يكرر
 *    المحتوى نفسه حرفياً.
 *  • `faq` **بلا سقف** — وهو سلوك مكتوب بقصدٍ في `components/sections/faq.tsx`
 *    («القسم قد يتكرر… وكل نسخة تصف أسئلتها هي»). ⚠ لكنه يُخرج عقدة `FAQPage`
 *    لكل نسخة، وتعدّدها على صفحة واحدة مأخذٌ في البيانات المهيكلة —
 *    **سؤالٌ للمالك في الموجز، لا قراراً يُتخذ هنا** فوق قرارٍ مكتوب.
 */
export const BLOCK_CATALOGUE: readonly BlockDef[] = [
  // ── كتلٌ حرّة: تُستعمل في أي صفحة وبأي عدد ──────────────────────────────
  {
    type: "rich-text",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "body"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: ["body"],
    styleKeys: ["background", "spacing", "hideOnMobile"],
  },
  {
    type: "page-hero",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub", "ctaLabel"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    /** 🔴 الإلزام هنا هو الإصلاح: بلا عنوان لا تُصيَّر — لا `<h1>` فارغ */
    requiredFields: ["title"],
    styleKeys: ["background", "spacing"],
  },
  {
    /**
     * ⚠ **م‑٧ أضافت `icon` داخل العنصر ولا شيء غيره.** القسم يحمل اليوم «كيف
     * نعمل» و«الضمانات الست» معاً (١٩ صفاً حيّاً)، وكلاهما في التصميم بأيقونة
     * لكل بطاقة — وكانت `CircleCheck` محفورةً في العارضة لكل عنصر.
     * والغياب يرجع إليها حرفاً، فصفحةٌ قائمة لا تتغيّر ببايت.
     *
     * وبلا صورة: بطاقةُ ميزةٍ نصٌّ ورمز، وصورةٌ فيها زخرفةٌ تزيد الوزن ولا
     * تضيف معنى — والقرار قابلٌ للنقض بسطرٍ هنا يوم يطلبه تخطيط.
     */
    type: "features",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub"],
    itemFields: ["title", "text"],
    nonTextFields: null,
    nonTextItemFields: ["icon"],
    requiredFields: ["items"],
    styleKeys: ["featuresLayout", "background", "spacing", "hideOnMobile"],
  },
  {
    type: "faq",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title"],
    itemFields: ["q", "a"],
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: ["items"],
    styleKeys: ["background", "spacing"],
  },
  {
    type: "cta-band",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "note"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    /** بلا حقلٍ إلزامي: نصوصها الافتراضية من الإعدادات، فهي تُصيَّر دائماً */
    requiredFields: [],
    styleKeys: ["background", "spacing", "hideOnMobile"],
  },

  // ── كتل النظام: بياناتها من الإعدادات، والنصّ تجاوزٌ اختياري ─────────────
  {
    /**
     * 🆕 **م‑٧: `items` اختيارية تتجاوز `SERVICES`.**
     *
     * التصميم يعطي كل خدمة صورةً، والبيانات كانت **في ملف TS** —
     * `lib/site-config.ts` — أي خارج ما تصله اللوحة إطلاقاً. و`requiredFields`
     * تبقى **فارغة** بقصد: بلا عنصرٍ واحد يعود القسم إلى `SERVICES` حرفاً
     * (نفس مذهب `hero.items` مع نقاط الثقة الأربع)، فلا صفحة قائمة تتغيّر.
     *
     * و`href` يدخل الفهرس كما في `route-rail` وللسبب نفسه (D-24). وثمنُه
     * مكتوبٌ في رأس `components/site/services.tsx`: البطاقة المتجاوَزة تفقد
     * ربطَ «إلغاء النشر يُطفئ الرابط» — وحدُّ خطرها ٤٠٤ داخلي.
     */
    type: "services-grid",
    role: "system",
    placement: "once-per-page",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub"],
    itemFields: ["title", "text", "href", "alt"],
    nonTextFields: null,
    nonTextItemFields: ["src", "icon"],
    requiredFields: [],
    styleKeys: ["background", "spacing"],
  },
  {
    /**
     * ⚠ **بلا `items` بقصد، وصورُها ليست نقصاً:** فئات الأسطول صفوفٌ في
     * `vehicle_classes` تُحرَّر من `/admin/fleet`، وصورتها العمود `image_url`
     * (موجودٌ منذ `0005`، ومُلئ في `0065`). فنسخُها إلى `items` كان سيخلق
     * مصدرين للسعة نفسها — والسعة تقرّر الأهلية في SQL (D-12).
     */
    type: "fleet",
    role: "system",
    placement: "once-per-page",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: [],
    styleKeys: ["background", "spacing"],
  },
  {
    type: "why-us",
    role: "system",
    placement: "once-per-page",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: [],
    styleKeys: ["whyUsLayout", "background", "spacing"],
  },
  {
    type: "contact",
    role: "system",
    placement: "once-per-page",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: [],
    styleKeys: ["background", "spacing"],
  },
  {
    /**
     * ⚠ **م‑٢ وسّعتها ولم تخترع بجانبها**: هي الكتلة الوحيدة التي تركّب
     * `BookingWidget`، ونسختان منها = ويدجتان على صفحة واحدة — فالشارة ونصّ
     * السهم وضمانات البطل حقولٌ فيها لا كتلةٌ ثانية.
     *
     * و`requiredFields` تبقى فارغة: البطل يُصيَّر دائماً لأن نصوصه الافتراضية
     * من الإعدادات، وإخفاؤه بحقلٍ ناقص كان سيُسقط الويدجت من الرئيسية.
     */
    type: "hero",
    role: "system",
    placement: "home-only",
    acceptsChildren: false,
    maxChildren: null,
    /**
     * `imageAlt` نصٌّ يُقرأ بقارئ الشاشة فيُترجم كأي نصّ — ولهذا هو هنا.
     *
     * 🔴 **وتصحيحٌ لما كان مكتوباً هنا (م‑٧، 2026-08-16):** كان السطر يقول إن
     * `src`/`poster`/`video` «حقولٌ اختيارية غير نصّية و`block_registry_check`
     * لا تعترضها» — وهو صحيحٌ في نصفه وكاذبٌ في نتيجته. القياس الحيّ بعد
     * `0064` أخرج **ثلاثة صفوف** لها في طابور الترجمة، لأن الفهرس لا يقرأ
     * `text_fields` ولا `block_registry` أصلاً: يقرأ كل مفتاحٍ قيمتُه نصّ.
     * الحجب الحقيقي صار **بالاسم** في `public.i18n_non_text_field` (‏`0065`)،
     * وإعلانُها هنا في `nonTextFields` هو ما يجعل المحرر يعرض لها منتقي وسائط.
     */
    /**
     * 🆕 **ن‑٤ — `typingPrefix` و`typingLines`، ونصّان لا رقم بينهما.**
     *
     * وكلاهما **نثرٌ يقوله إنسان** فيدخل الفهرس كأي نصّ: الجزء الثابت اسمُ
     * الخدمة، والجمل المتناوبة وعودٌ تُقرأ. أما إيقاع الأثر (سرعة · مهلة ·
     * محو · تكرار) فرموزٌ في `style` لا حقولٌ هنا — والمبرر مكتوبٌ في §٥.
     *
     * 🔒 **والعلاقة بـ`headline` علاقة تجاوزٍ لا تكرار:** بلا جملةٍ واحدة في
     * `typingLines` يبقى العنوان `headline` حرفاً بحرف وبلا عقدة DOM زائدة.
     * وبأول جملة يصير العنوان `typingPrefix` ثابتاً + الجملة متحرّكة.
     * فالحقل الذي يُحفظ هو الحقل الذي يعمل (الذهبية ١٨)، ونصّ المساعدة في
     * اللوحة يقول ذلك صراحةً على الحقول الثلاثة.
     */
    textFields: ["badge", "headline", "typingPrefix", "typingLines", "sub", "scrollLabel", "imageAlt"],
    itemFields: ["title"],
    nonTextFields: ["src", "poster", "video"],
    nonTextItemFields: null,
    requiredFields: [],
    /**
     * ⚠ **ولا `background` ولا `spacing` معها**: البطل يرسم أرضيته بنفسه
     * (صورة + حجابان)، وغلافٌ ملوّن حوله يضع لوناً صمّاً فوق الصورة. فما
     * دخل هنا هو **مقابض الحركة وحدها** — وهي التي تصف داخل الكتلة.
     */
    styleKeys: ["typingSpeed", "typingHold", "typingErase", "typingLoop"],
  },

  // ── الكتلتان الجديدتان ────────────────────────────────────────────────
  {
    /** جواب «التداخل» في الخارطة — تخطيطٌ لا محتوى، فلا نصَّ لها إطلاقاً */
    type: "columns",
    role: "layout",
    placement: "any",
    acceptsChildren: true,
    maxChildren: 4,
    textFields: [],
    itemFields: null,
    /** كتلة تخطيط لا تحمل وسائط كما لا تحمل نصّاً — يفرضه `layout-carries-media` */
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: [],
    styleKeys: ["background", "spacing", "hideOnMobile"],
  },
  {
    /**
     * `alt` **نصٌّ قابل للترجمة** لا سمةٌ تقنية — وإغفاله هو الخطأ المعتاد.
     * و`src` مسارٌ في دلو `media` (قائمٌ منذ `0003`)، ولا يقبل نطاقاً خارجياً.
     *
     * ⚠ وإعلانه في `nonTextFields` **شرطٌ لا زينة** بعد `0065`: القاعدة (د) في
     * `block_registry_check` تقرأ `requiredFields` على اتحاد القوائم، فحذفُه
     * من هنا يجعل الكتلة تُرفض بـ`required-field-unfillable:src`.
     */
    type: "image",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["alt", "caption"],
    itemFields: null,
    nonTextFields: ["src"],
    nonTextItemFields: null,
    requiredFields: ["src", "alt"],
    styleKeys: ["spacing", "hideOnMobile"],
  },

  // ── كتل م‑٢ الثلاث — تركيب التصميم، ولا رابعة ─────────────────────────
  {
    /**
     * 🔴 **قرار بدر ٤ انتهى مدّته — 2026-08-17، والسبب أن مبرره انتهى قبله.**
     *
     * كانت الشعارات **بيانات نظام** في `site_settings.fleetBrands`، والمبرر
     * المكتوب أن «`src` داخل عنصر قائمة شكلٌ لم يُقنَّن في العقد». وم‑٧ **قنّنته
     * بعينه**: `NON_TEXT_FIELD_NAMES` تحجب `src` بالاسم على مستوى العنصر
     * (‏`0065`)، و`route-rail` و`services-grid` تحملان صور عناصرهما فعلاً.
     * فالقيد الذي منع النقل زال ⇒ **والقرار يزول معه**، لا يبقى بحكم العادة.
     *
     * وبقي في م‑٧ مبرر ثانٍ — **نطاقٌ لا بنية**: «قائمةٌ واحدة تخدم كل صفحةٍ
     * تحمل الشريط، فنقلُها إلى `items` يجعل ماركةً تُضاف في صفحةٍ وتغيب في
     * أخرى». وهذا يُعالَج بالاحتياط لا بالمنع: **`items` تتجاوز والإعدادات
     * تحتيها** — نفس مذهب `services-grid` مع `SERVICES` و`hero.items` مع نقاط
     * الثقة الأربع حرفاً. فالصفحة التي لا تُحرَّر ترى القائمة الواحدة كما
     * كانت، والصفحة التي حُرِّرت تعرض ما كُتب فيها.
     *
     * و`requiredFields` تبقى **فارغة** لهذا بعينه: بلا عنصرٍ واحد يعود القسم
     * إلى `settings.fleetBrands` حرفاً، فلا صفحة قائمة تتغيّر ببايت.
     *
     * ⚠ **وثمنُ الاحتياط مكتوبٌ بلا تجميل:** من حذف **كل** العناصر تعود إليه
     * قائمة الإعدادات بدل أن يفرغ الشريط. وهو نفس ثمن `services-grid` المشحون،
     * ونصُّ المساعدة في اللوحة يقوله صراحةً. والبديل — قائمةٌ فارغة تعني «لا
     * شعارات» — كان يجعل كل كتلةٍ جديدة تُضاف على صفحةٍ أخرى تُصيَّر شريطاً
     * فارغاً حتى يُعاد إدخال العشرة بيداً.
     *
     * 🔴 **و`disclaimer` حُذف كاملاً بقرار المالك (2026-08-17).** كان حقلاً
     * ثالثاً أضافته `0072`: شرطُ استعمالٍ يعيد نصّه الافتراضي عند الفراغ، فيملك
     * المالك صياغته ولا يملك حذفه. **عُرض عليه الأمر بمخاطره فقرّر: خانةٌ
     * واحدة، ويُحذف الثاني.** وهو قرار منتجٍ لا هندسة (البند ٣ في
     * `docs/STANDING-ORDERS.md`) — فيُنفَّذ بلا تحوّطٍ في الواجهة.
     * ونصُّ التنويه المحذوف محفوظٌ في
     * `docs/phase-briefs/OWNER-NOTES-2026-08-16.md` كي لا يُعاد اختراعه إن
     * طلبه يوماً؛ **ولا نسخة منه في الكود** بطلبه.
     *
     * و`note` **لا يُمسّ**: نثرُ المالك فيه صفٌّ حيّ على الرئيسية.
     */
    type: "logo-strip",
    role: "system",
    placement: "once-per-page",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "note"],
    /**
     * `name` اسمُ الماركة — نصٌّ يُترجَم (وكان مجمَّداً على العربية في اللغتين
     * حين كان في `site_settings`، فالنقل يفتح ترجمته لا يقفلها).
     * و`href` يدخل الفهرس كما في `route-rail` وللسبب نفسه (D-24).
     * و`alt` **إلزامٌ بنيوي** لا اختيار: القاعدة (٤) في `block_registry_check`
     * ترفض كتلةً تعلن `src` داخل العنصر ولا تعلن `alt` معه.
     */
    itemFields: ["name", "href", "alt"],
    nonTextFields: null,
    nonTextItemFields: ["src"],
    requiredFields: [],
    /**
     * ⚠ **ولا `icon` في العنصر**: الشعار صورةُ علامةٍ بعينها، ورمزٌ من قائمةٍ
     * مغلقة لا يقوم مقامها — وأي أيقونة تُعرض مكان شعار «مرسيدس» تقول شيئاً
     * آخر. وبلا `src` يُعرض **الاسم نصّاً** كما اليوم، وهو الاحتياط الصحيح.
     */
    styleKeys: [
      "marqueeSpeed",
      "marqueeDirection",
      "marqueePause",
      "logoEffect",
      "logoLink",
      "background",
      "spacing",
      "hideOnMobile",
    ],
  },
  {
    /**
     * 🔴 **`requiredFields: ["items"]` هو تنفيذ قرار بدر ٣ حرفياً:** الكتلة
     * تُشحن والأرقام لا. شريطٌ بلا عنصر واحد **لا يُصيَّر إطلاقاً** بدل أن
     * يعرض إطاراً فارغاً — فالمالك يملؤه بما يملك أو يتركه فيغيب.
     *
     * و`value` حقلٌ نصّي **بقصد** لا رقمٌ في `style`: الرقم يُكتب ١٢٬٤٠٠
     * بالعربية و12,400 بالإنجليزية، فوضعُه خارج الفهرس يجمّده على صيغة لغةٍ
     * واحدة في اللغتين. والثمن سطرٌ في طابور الترجمة.
     */
    type: "stat-band",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title"],
    itemFields: ["value", "suffix", "label"],
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: ["items"],
    styleKeys: ["background", "spacing", "hideOnMobile"],
  },
  {
    /**
     * **بلا سعر** (قرار بدر ١): لا حقل سعر في `pages` أصلاً، والسعر المعروض
     * الذي تخالفه الحاسبة بعد ثانيتين يهدم الجملة التي بُني عليها المنتج.
     * **وهذا القرار قائمٌ ولم يُنقَض.**
     *
     * 🔴 **وقرار «بلا صورة» نُقض صراحةً — 2026-08-16، بأمر بدر:**
     * «ضيف الأعمدة المطلوبة بحيث يمكن التحكم في كل شيء من خلال لوحة التحكم
     * بما فيها الصور والأيكونات». وكان مكتوباً هنا أن الصورة «قيدُ عقدٍ لا
     * نقصُ تنفيذ» لأن `src` داخل عنصر قائمة شكلٌ لم يُقنَّن — **فقُنِّن**:
     * `nonTextItemFields` أعلاه، و`i18n_non_text_field` في `0065`، والعقد
     * كاملاً في `lib/item-fields-types.ts`. فالبطاقة تحمل اليوم صورةً
     * ونصَّها البديل وأيقونة، وكلها من اللوحة.
     *
     * و`alt` **نصٌّ يدخل الفهرس** ولا يلحق بالثلاثة: من لا يرى الصورة يسمعه،
     * وجوجل يفهرسه — وضمُّه إلى حزمة الوسائط هو الخطأ المعتاد.
     *
     * و`href` يدخل الفهرس مع بقية حقول العنصر، وهذا **صحيح لا عرَض**: المسار
     * العربي بلا بادئة والإنجليزي تحت `/en` (D-24)، فالوجهة تختلف بالفعل
     * باختلاف اللغة.
     */
    type: "route-rail",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "sub", "note"],
    itemFields: ["name", "href", "duration", "distance", "alt"],
    nonTextFields: null,
    nonTextItemFields: ["src", "icon"],
    requiredFields: ["items"],
    styleKeys: ["background", "spacing", "hideOnMobile"],
  },

  // ── كتل المستندات (م‑١٠) — أربعٌ تعالج عيباً بنيوياً لا تحريرياً ────────
  {
    /**
     * **`requiredFields` فارغة و«لا يُصيَّر بلا بنود» في العارضة لا في السجل.**
     *
     * والفرق ليس أسلوبياً: البوابة `blockRenders` تحكم على **محتوى الكتلة
     * وحدها**، وفهرسُ الصفحة لا محتوى له أصلاً — شرطُ ظهوره هو وجود كتل `clause`
     * **أخرى** على الصفحة. فحقلٌ إلزامي هنا كان سيَعِد بما لا يقيسه: كتلةٌ
     * فيها `title` تمرّ البوابة ثم تُصيَّر فارغة. والحكم الصحيح مكانه العارضة
     * التي ترى الصفحة كلها، وهي ترجع `null` — لا صندوق فهرسٍ بلا سطر.
     *
     * و`once-per-page`: فهرسان على صفحةٍ واحدة تنقّلٌ مكرَّر يقول للقارئ إن
     * أحدهما ناقص.
     */
    type: "page-toc",
    role: "content",
    placement: "once-per-page",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: [],
    styleKeys: ["background", "spacing", "hideOnMobile"],
  },
  {
    /**
     * 🔴 **`anchor` في `nonTextFields` هو تنفيذ قرار المرساة** (‏العقد الثاني
     * `lib/item-fields-types.ts` §١٠): معرّفٌ لا نثر، فلا يدخل الفهرس ولا
     * تستبدله `i18n_apply` — ولولا ذلك لكان مترجمٌ مجتهد يعرّب `cancellation`
     * فتنكسر كل الروابط المرسلة على `/en` وحدها، بلا خطأ ولا سجل.
     *
     * و`requiredFields: ["title"]` لا `["title","anchor"]`: المرساة **لا تكون
     * ناقصة أبداً** (الغياب يعني معرّف الصفّ)، وإلزامُها كان سيُخفي بنداً
     * كاملاً من صفحة سياسات لأن حقلاً تقنياً فارغ — وهو أسوأ ما يمكن أن يفعله
     * حقلٌ اختياري بطبيعته.
     *
     * ⚠ و`num` **نصٌّ يُترجَم** ولا يلحق بالمرساة: «٤» و«4» صيغتان تختلفان
     * باللغة — نفس مبرر `stat-band.value` حرفاً.
     */
    type: "clause",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["num", "title", "body"],
    itemFields: null,
    nonTextFields: ["anchor"],
    nonTextItemFields: null,
    requiredFields: ["title"],
    styleKeys: ["background", "spacing"],
  },
  {
    /**
     * **الترويسة أعلى والخلايا داخل العنصر** — وهو ما يجعل الجدول قابلاً
     * للعنونة أصلاً: `<sectionId>.h2` لعنوان العمود، و
     * `<sectionId>.items.<_k>.c2` للخلية. ولو كان الصفّ نصّاً واحداً بفواصل
     * لصار عموداً واحداً في الفهرس، ولعاد من المترجم بفواصل في غير مواضعها.
     *
     * ولا `hideOnMobile` في مقابضه: جدولُ نوافذ الإلغاء هو المعلومة التي
     * يفتحها الزائر من هاتفه بعينها، وإخفاؤه على الجوال يعني إخفاءها عن
     * أغلبية القرّاء. والتمرير الأفقي **داخل حاويته** هو الجواب لا الإخفاء.
     */
    type: "table",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "note", "h1", "h2", "h3", "h4"],
    itemFields: ["c1", "c2", "c3", "c4"],
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: ["items"],
    styleKeys: ["background", "spacing"],
  },
  {
    /**
     * `body` إلزامي: تنبيهٌ بلا نصّ صندوقٌ ملوّن فارغ — والتلوين بلا رسالة
     * ضجيجٌ يعلّم القارئ أن يتخطى الصناديق الملوّنة كلها.
     *
     * ولا `background` في مقابضه (‏`tone` مكانها): خلفيةٌ مختارة تحارب تلوين
     * النبرة في العنصر نفسه، فيخرج صندوقٌ لونُه يقول شيئاً وحدُّه يقول غيره.
     */
    type: "callout",
    role: "content",
    placement: "any",
    acceptsChildren: false,
    maxChildren: null,
    textFields: ["title", "body"],
    itemFields: null,
    nonTextFields: null,
    nonTextItemFields: null,
    requiredFields: ["body"],
    styleKeys: ["tone", "spacing", "hideOnMobile"],
  },
] as const;

/**
 * 🔴 **`NON_TEXT_CONTENT_FIELDS` حُذف في م‑٧ ولم يُترك بجانب خلفه.**
 *
 * كان يقول نصّاً إن `src` «المفتاح غير النصّي **الوحيد**» — وهو وصفٌ خاطئ منذ
 * `0064` (‏`poster` و`video` موجودان)، وكاذبٌ في اتجاهه الآخر: **لا أثر له على
 * الفهرس إطلاقاً** (مستهلكه الوحيد كان حيلةً في `block_registry_check`).
 * وخلفُه `NON_TEXT_FIELD_NAMES` في `lib/item-fields-types.ts` §١ — ومعه
 * مرآتُه الحيّة `public.i18n_non_text_field`. قائمتان تنحرفان، والنمط ٤ في
 * `LESSONS.md` مكتوبٌ في هذا الملف بالذات.
 */
export { NON_TEXT_FIELD_NAMES, type NonTextFieldName } from "@/lib/item-fields-types";

// ---------------------------------------------------------------------------
// (١١) بوابة النشر — رموزٌ من القاعدة، لا فحصٌ في المتصفح
// ---------------------------------------------------------------------------

/**
 * **القرار:** `page_publish_blockers(p_page uuid)` في Postgres تُرجع **رموزاً**،
 * وزر النشر معطَّل ما دامت غير فارغة.
 *
 * **المبرر:** «لا ينشر نصف صفحة بالخطأ» شرطٌ في الطلب، وفحصٌ في JavaScript
 * يتخطاه أي `update` مباشر. ورمزٌ لا جملة لأن الخادم يرسل رمزاً والواجهة
 * تترجمه (قاعدة المشروع، وإلا ظهرت العربية على `/en`).
 *
 * **عاقبة النقض:** صفحةٌ فارغة أو بعنوان فارغ تدخل `sitemap.xml` وتُعلَن
 * للزاحف — وسحبُها من النتائج أبطأ بكثير من منعها.
 */
export const PUBLISH_BLOCKER_CODES = [
  "no-blocks", // لا كتلة واحدة
  "all-blocks-empty", // كل الكتل تُصيَّر null ⇒ صفحة بيضاء
  "missing-required", // كتلة ينقصها حقل إلزامي
  "empty-title", // عنوان الصفحة فارغ
  "no-meta-description", // بلا وصف سيو — تحذيرٌ يمنع لأن السيو هو المنتج
  "orphan-child", // كتلة ابنة بلا أب (أو أبٌ حُذف)
  "depth-exceeded", // حفيدٌ — يخالف §٣
  "slug-conflict", // §٨
  "duplicate-singleton", // كتلة `once-per-page` مكررة
  "home-only-misplaced", // `hero` خارج الرئيسية
] as const;
export type PublishBlockerCode = (typeof PUBLISH_BLOCKER_CODES)[number];

/** رموز الخطأ التي تسافر في الـ query string من إجراءات المنشئ */
export const BUILDER_ERROR_CODES = [
  "env", // لا متغيرات بيئة
  "forbidden", // ليس `admin`
  "save", // صفر صفوف بعد الكتابة (فخ RLS)
  "stale-revision", // حُرِّرت اللقطة من جلسة أخرى
  "publish-blocked", // البوابة رفضت — التفصيل في `PublishBlockerCode`
  "block-unknown", // نوع كتلة غير مسجَّل
  "block-placement", // موضع مخالف
  "item-key", // `_k` مكرر أو مخالف للنمط
  "template-shape", // قالبٌ مستورَد لا يطابق القواعد
] as const;
export type BuilderErrorCode = (typeof BUILDER_ERROR_CODES)[number];

// ---------------------------------------------------------------------------
// (١٢) ما **لا** تبنيه المرحلة ١٣ — مؤجَّلٌ بسببٍ مكتوب (D-39: الحذف ممنوع)
// ---------------------------------------------------------------------------

/**
 * كل بندٍ هنا مذكورٌ في الرؤية أو الخارطة، **ولم يُحذف** — أُجّل بسببه:
 *
 * ١) **CSS حرّ لكل صفحة وللموقع** (‏`VISION.md:97`) — مؤجَّل إلى المرحلة ١٤.
 *    **السبب:** نصٌّ يُحقن في `<head>` مصدرُه صفٌّ في القاعدة، فيكسر ثلاثة
 *    أشياء دفعةً: قواعد RTL المنطقية (`ms/me/ps/pe`) التي يقوم عليها التحوّل
 *    إلى `/en`، وثيمَ العلامة الذي تُطلق به النسخة الثانية (**D-01**)، وسطحَ
 *    حقنٍ لا يمرّ على مراجعة. والبديل المشحون: رموز ثيم لكل كتلة (§٥) — وهي
 *    ٩٠٪ مما يطلبه المالك عملياً («التحكم في الألوان»).
 * ٢) **معرض قوالب وسوقٌ لها** — يُشحن التصدير والاستيراد (لقطة `page_revisions`
 *    ملفَ `json`) بلا معرض. **السبب:** المعرض واجهةٌ فوق قدرةٍ لا وجود لها بعد،
 *    وقيمته صفر قبل وجود قالبٍ ثانٍ.
 * ٣) **الاستيراد من منشئات خارجية** — الشكل المستورَد يجب أن يطيع قواعد §١–§٥،
 *    فما لا يطيعها يُرفض برمز `template-shape` ولا يُحوَّل بالتخمين.
 * ٤) **تداخلٌ أعمق من مستوى** (§٣) — مؤجَّل حتى يطلبه تخطيطٌ حقيقي.
 * ٥) **مكتبة وسائط كاملة** (وسوم · قصّ · أحجام متعددة) — يُشحن منها المطلوب
 *    لكتلة `image` وحده: رفعٌ إلى دلو `media` القائم واختيارٌ من قائمته.
 * ٦) **ترجمة `style`** — لا تُترجَم أبداً، بقرارٍ لا بتأجيل (§٥).
 * ٧) **مسار عام جديد** — صفر ملف تحت `app/` خارج `/{slug}` القائم (**D-24**).
 *
 * 🔴 **ثمنٌ يخصّ الطريق لا النطاق، ويجب أن يُقرأ قبل البدء:**
 * محرر المحتوى القائم يعيد بناء `content` **كاملاً** من النموذج في كل حفظ
 * (‏`sectionContentFromForm` — `switch` يُسقط كل مفتاح لا يعرفه). فأي كتلة تكتب
 * `style` أو `_k` ثم تُحفَظ من `/admin/content/[id]` القديم **تفقدهما صامتة**.
 * فإما أن يصير المنشئ **الكاتب الوحيد** لأنواع الكتل التي يديرها، وإما أن
 * يُوسَّع المحرر القديم ليحفظ المفتاحين. **الثالث — تركهما يكتبان معاً — عطبٌ
 * مؤكد لا احتمال.**
 */
export const PHASE_13_OUT_OF_SCOPE = [
  "custom-css",
  "template-gallery",
  "external-import",
  "deep-nesting",
  "media-library-full",
  "style-translation",
  "new-public-routes",
] as const;

// ---------------------------------------------------------------------------
// (١٣) أنواع الشاشة — ما تقرؤه مكوّنات المنشئ
// ---------------------------------------------------------------------------

/** كتلة كما يعرضها المنشئ (لقطةً أو حيّة) */
export type BuilderBlock = {
  id: string;
  pageId: string;
  parentId: string | null;
  type: BuilderBlockType;
  content: Record<string, unknown>;
  sort: number;
  visible: boolean;
  /** الأبناء مرتّبين — فارغة لغير `layout` */
  children: BuilderBlock[];
};

export type BuilderPage = {
  id: string;
  slug: string;
  kind: PageKind | "landing";
  title: string;
  published: boolean;
  /** اللقطة المفتوحة للتحرير — `null` = لا مسودة، الحيّ هو المعروض */
  draftRevisionId: string | null;
  /** فرقٌ بين المسودة والمنشور؟ يُحسب في القاعدة لا بمقارنة في المتصفح */
  hasUnpublishedChanges: boolean;
  blocks: BuilderBlock[];
  access: BuilderAccess;
};

/**
 * صفٌّ في كتالوج القاعدة `block_registry`. **مرآةٌ لـ`BLOCK_CATALOGUE`**،
 * والاختبار يقارنهما ويفشل عند الانحراف — نسختان تنحرفان يوم تُضاف كتلة
 * (النمط ٤ في `LESSONS.md`).
 */
export type BlockRegistryRow = {
  type: string;
  role: BlockRole;
  placement: BlockPlacement;
  accepts_children: boolean;
  max_children: number | null;
  text_fields: string[];
  item_fields: string[] | null;
  required_fields: string[];
  /** 🆕 م‑٧ (‏`0065`) — مرآة `nonTextFields`/`nonTextItemFields` أعلاه */
  non_text_fields: string[] | null;
  non_text_item_fields: string[] | null;
  enabled: boolean;
};

/** بحثٌ سريع عن تعريف كتلة — المصدر الوحيد في طبقة TypeScript */
export function blockDef(type: string): BlockDef | null {
  return BLOCK_CATALOGUE.find((b) => b.type === type) ?? null;
}

/**
 * هل تُصيَّر هذه الكتلة أصلاً؟ **نفس المنطق الذي تنفّذه العارضة** — يُستعمل في
 * المعاينة وفي بوابة النشر معاً، فلا يختلف حكمُ الشاشة عن حكم الصفحة.
 */
export function blockRenders(type: string, content: Record<string, unknown>): boolean {
  const def = blockDef(type);
  if (!def) return false;
  for (const field of def.requiredFields) {
    const value = content[field];
    if (field === ITEMS_FIELD) {
      if (!Array.isArray(value) || value.length === 0) return false;
      continue;
    }
    if (typeof value !== "string" || value.trim() === "") return false;
  }
  return true;
}
