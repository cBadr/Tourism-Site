/**
 * عقد نظام المحتوى (المرحلة ٢) — نموذج «الأقسام»:
 * كل صفحة عامة = صف في `pages` + أقسام مرتبة في `sections` بمحتوى JSONB.
 * هذا الملف هو المرجع الأوحد لأنواع الأقسام وأشكال محتواها — قاعدة البيانات
 * والواجهة العامة ولوحة التحكم والـ Page Builder لاحقاً كلهم يلتزمون به.
 */

import type { PageSeoExtras } from "@/lib/seo-types";
import type { ItemMedia } from "@/lib/item-fields-types";

/**
 * أنواع الصفحات — والرابع `landing` من المرحلة ١٣ (‏`0058_page_builder.sql` §١).
 *
 * ولماذا نوعٌ مستقل لا إعادةُ استعمال `static`؟ لأن ثلاثة مستهلكين يفرّقون فعلاً
 * بين «سياسة الخصوصية» و«صفحة هبوط تسويقية»: `PRIORITY` في `app/sitemap.ts`،
 * و`pagePublicPath` في `lib/seo/site-paths.ts`، وقائمة `/admin/content`.
 *
 * ⚠ **وإضافته هنا خطأُ بناءٍ مقصود** في كل `switch` بلا `default` وكل
 * `Record<PageKind, …>` — فنسيانُ التعامل معه يظهر في `tsc` لا صامتاً في الإنتاج
 * (العقد `lib/page-builder-types.ts` §٨ يسمّي هذه «فائدة مجانية مقصودة»).
 */
export type PageKind = "home" | "service" | "corridor" | "static" | "landing";

/**
 * ميتاداتا الصفحة — عمود `pages.meta` من نوع `jsonb`.
 *
 * الحقول الأربعة الجديدة تأتي من عقد `lib/seo-types.ts` بلا إعادة كتابة، وهي
 * **`Partial` بقصد**: نصّ العقد يقول إن المفتاح الغائب في صفٍّ قديم ليس خطأً بل
 * «غير مضبوط» يسقط إلى الافتراضي الآمن. فلو جُعلت مطلوبةً لصار كل صفٍّ مبذور
 * وكل كائن `meta` في `lib/default-content.ts` خطأَ بناء، ولاحتاج الأمر ترحيل
 * بيانات يكتب قيمةً في صفوف لم يلمسها المالك — وهو ما استبعده العقد صراحةً.
 *
 * ولذلك يقرأ كل مستهلك بمقارنة صريحة (`meta.noindex === true`) لا بصدق القيمة:
 * `undefined` و`null` و`false` ثلاثتها تعني «لم يُطلَب المنع».
 */
export type PageMeta = {
  /** يدخل في قالب العنوان العام %s */
  title: string | null;
  description: string | null;
} & Partial<PageSeoExtras>;

export type Page = {
  id: string;
  slug: string;
  kind: PageKind;
  title: string;
  meta: PageMeta;
  published: boolean;
  sort: number;
  /**
   * ختم آخر تعديل بصيغة ISO — مصدره العمود `updated_at` في جدول `pages` الذي
   * يحدّثه مشغّل `pages_touch_updated_at` (`0003_content.sql:23,27-30`).
   *
   * اختياري **هنا** لأن مُحمّل لوحة التحكم (`app/admin/content/loader.ts`) يبني
   * صفحاته لغرض التحرير ولا يحتاجه؛ أما الواجهة العامة فتضمنه عبر `PublicPage`.
   */
  updatedAt?: string | null;
};

export type SectionType =
  | "hero" // الرئيسية فقط — يقرأ الهوية من الإعدادات
  | "page-hero" // ترويسة صفحة داخلية: عنوان + نص + CTA اختياري
  | "services-grid" // شبكة الخدمات الست (بياناتها من site-config حالياً)
  | "fleet" // فئات السيارات الأربع
  | "why-us" // نقاط القيمة
  | "features" // قائمة مزايا عامة بعناصر حرة
  | "rich-text" // فقرات نصية (سطر فارغ = فقرة جديدة)
  | "faq" // أسئلة شائعة — تُصدَّر أيضاً كـ JSON-LD
  | "cta-band" // شريط دعوة للحجز
  | "contact" // قنوات التواصل من الإعدادات
  // ── كتلتا المرحلة ١٣ ────────────────────────────────────────────────────
  | "columns" // كتلة تخطيط: لا نصَّ لها، وأبناؤها صفوفٌ في `sections` بـ`parent_id`
  | "image" // صورة من دلو `media` + نصٌّ بديل **قابل للترجمة**
  // ── كتل تركيب التصميم (م‑٢) — الثلاث وحدها، ولا رابعة ───────────────────
  | "logo-strip" // شريط شعارات ماركات الأسطول — **نظامية**: القائمة من الإعدادات
  | "stat-band" // شريط أرقام: قيمة + لاحقة + تسمية، كلها محرَّرة
  | "route-rail" // سكة بطاقات المسارات — بلا أسعار (قرار بدر ١)
  // ── كتل المستندات (م‑١٠) — أربعٌ وحدها، وعلّتها مقيسة ────────────────────
  //
  // 🔴 الصفحات الثابتة اليوم مبنيةٌ من `rich-text` متتالية وحدها: الشروط **١١**
  //    والخصوصية **١٠** وسياسة الاسترداد **٩**. وعشر فقراتٍ متتالية **جدارُ نصٍّ
  //    بالبناء** — إعادةُ صياغة النثر لا تصلحه، لأن القيد مفرداتٌ لا أسلوب.
  //    والمحتوى نفسه جيّد: فيه نوافذ إلغاء (٢٤/٤٨/٧٢ ساعة) وخصومات (٢٥٪/٥٠٪)
  //    وانتظارٌ مجاني (١٥/٣٠/٦٠ دقيقة) — **أرقامٌ مدفونة في نثر**. و«كم أخسر لو
  //    ألغيت غداً؟» يجب أن يُجاب في ثانيتين، وهو اليوم موزّعٌ على ثلاث فقرات.
  //    **وذاك جدولٌ لا فقرة.**
  | "page-toc" // فهرس الصفحة — يُبنى آلياً من بنود الصفحة نفسها
  | "clause" // بند مرقَّم بمرساة ثابتة — «§٤» يُرسَل رابطاً
  | "table" // جدول — نوافذ الإلغاء والانتظار والرسوم
  | "callout"; // تنبيه بارز — البند الذي يُغضب حين يُكتشف متأخراً

/** أشكال محتوى JSONB لكل نوع قسم — الحقول الاختيارية تُعرض فقط عند وجودها */
export type SectionContentMap = {
  /**
   * توسيعٌ لا كتلة جديدة (م‑٢): الشارة ونصّ السهم وضمانات البطل صارت حقولاً
   * يملكها المالك بدل أن تكون محفورة في `components/site/hero.tsx`.
   *
   * وكلها **اختيارية بقصد**: الغياب يُخرج ما كان الكود يُخرجه حرفاً بحرف
   * (الشارة من `company.activity` والضمانات الأربع من مساحة `site.hero`)،
   * فالصفحات الحيّة لا تتغيّر ببايت حتى يكتب المالك أول تجاوز.
   */
  hero: {
    headline?: string;
    sub?: string;
    /**
     * ── ن‑٤: أثر الكتابة — **قناعٌ فوق نصٍّ كامل من الخادم، لا حقنُ حروف** ──
     *
     * 🔴 **القرار الذي يفصل بين تنفيذٍ سليم وآخر يكلّف الصفحة أغلى ما فيها:**
     * الحقن حرفاً حرفاً يترك `<h1>` فارغاً أو ناقصاً في HTML الذي يصل الزاحف —
     * وهو أهم عنصرٍ في الصفحة كلها — **ويجعل قارئ الشاشة ينطق كل حرفٍ على
     * حدة**. فالنصّ يُصيَّر كاملاً على الخادم، ولا تتحرك إلا رقعةُ قصٍّ فوقه.
     *
     * `typingPrefix` — الجزء **الثابت** الذي لا يُعاد كتابته في أي دورة
     * («ايجار ليموزين»). واسمُ العلامة لا يومض.
     *
     * `typingLines` — الجُمل المتناوبة، **سطرٌ لكلٍّ منها**. وهي نصٌّ واحد لا
     * `items` بقصد: `items` مأخوذةٌ في هذه الكتلة لضمانات البطل، وسطرٌ في
     * صندوقٍ واحد أخفّ على من يكتب ثلاث جملٍ قصيرة من ثلاث بطاقات.
     *
     * والحقلان **اختياريان كإخوتهما**: بلا جملةٍ واحدة يبقى العنوان
     * `headline` حرفاً بحرف، بلا مكوّن عميل وبلا عقدة DOM زائدة.
     */
    typingPrefix?: string;
    typingLines?: string;
    /** نصّ الشارة فوق العنوان — الفارغ يرجع إلى نشاط الشركة من الإعدادات */
    badge?: string;
    /** نصّ سهم «اكتشف» أسفل البطل — الفارغ يُخفي السهم كله */
    scrollLabel?: string;
    /**
     * ── وسائط البطل — أربعة حقول **غير نصّية**، ولا تدخل فهرس الترجمة ───────
     *
     * 🔴 **وهذا صار صحيحاً في `0065` لا قبلها.** كان مكتوباً هنا أن `src`
     * «مقنَّن في العقد» عبر `NON_TEXT_CONTENT_FIELDS` — والقياس الحيّ بعد
     * `0064` أخرج `src` و`poster` و`video` **صفوفاً في طابور الترجمة**، لأن
     * الفهرس لا يقرأ ذلك الثابت ولا `block_registry` أصلاً. والحجب اليوم
     * **بالاسم** في `public.i18n_non_text_field` (‏`NON_TEXT_FIELD_NAMES`)،
     * ويسري على المستوى الأعلى وداخل العنصر معاً.
     *
     * والحارس هو نفسه: `safeMediaSrc` — مسارٌ داخلي أو مضيف الوسائط وحده.
     *
     * ولماذا في الكتلة أصلاً لا محفورةً في العارضة؟ القيد غير القابل للتفاوض:
     * «التحكم في كامل الموقع من اللوحة». ومسارٌ محفور في JSX يعني أن نسخة
     * الـwhite-label الثانية تُطلق بصورة الأولى (‏D-01).
     *
     * والأربعة اختيارية: بلا `src` لا تُصيَّر طبقة الوسائط أصلاً ويعود البطل
     * إلى شكله السابق حرفاً — فصفحةٌ قائمة لا تتغيّر ببايت.
     */
    src?: string;
    /** نصّ بديل للصورة. الفارغ ⇒ `alt=""` أي زخرفة، لا صورة بلا وصف */
    imageAlt?: string;
    /** غلاف الفيديو — يسقط إلى `src` نفسها فلا يُحمَّل ملفٌّ ثانٍ */
    poster?: string;
    /** مصدر الفيديو. الفارغ ⇒ صورةٌ ساكنة، وهو مسارٌ سليم لا نقص */
    video?: string;
    /** ضمانات البطل — الفارغة ترجع إلى نقاط الثقة الأربع المترجَمة */
    items?: { title: string }[];
  };
  "page-hero": { title: string; sub?: string; ctaLabel?: string };
  /**
   * شبكة الخدمات. **و`items` تجاوزٌ اختياري كامل** (م‑٧): بلا عنصرٍ واحد
   * تُقرأ الخدمات الست من `SERVICES` ومن الصفحات المنشورة حرفاً كما كانت،
   * ومع أول عنصرٍ يملك المالك البطاقات كلها — نصَّها وصورتها وأيقونتها ووجهتها.
   *
   * ⚠ **وثمنٌ يُقرأ قبل الملء:** البطاقة المتجاوَزة تأخذ وجهتها من `href`
   * المكتوب لا من `getPagesByKind("service")`، فإلغاءُ نشر صفحة خدمة لم يعد
   * يُطفئ رابطها بل يجعله ٤٠٤ داخلياً. والحدّ محفوظ: `internalPath` يرفض كل
   * ما ليس مساراً داخلياً، فلا وجهة خارجية من قلب الرئيسية.
   */
  "services-grid": {
    title?: string;
    sub?: string;
    items?: ({ title: string; text?: string; href?: string } & ItemMedia)[];
  };
  fleet: { title?: string; sub?: string };
  "why-us": { title?: string; sub?: string };
  /**
   * مزايا/ضمانات. و`icon` اسمٌ من `ITEM_ICON_NAMES` — الغياب يرجع إلى
   * `CircleCheck` التي كانت محفورة في العارضة، والمجهول **يغيب ولا ينهار**.
   */
  features: {
    title?: string;
    sub?: string;
    items: ({ title: string; text?: string } & Pick<ItemMedia, "icon">)[];
  };
  "rich-text": { title?: string; body: string };
  faq: { title?: string; items: { q: string; a: string }[] };
  "cta-band": { title?: string; note?: string };
  contact: { title?: string; sub?: string };
  /**
   * كتلة تخطيط بلا محتوى إطلاقاً — أبناؤها **صفوفٌ** في `sections` يربطها
   * `parent_id`، لا مفاتيح داخل `jsonb` (العقد §٣: العمق في الصفوف لا في
   * الحقول، وإلا اختفى النصّ من فهرس الترجمة بدل أن ينفجر).
   */
  columns: Record<string, never>;
  /** `alt` نصٌّ قابل للترجمة لا سمةٌ تقنية — وإغفاله هو الخطأ المعتاد (العقد §١٠) */
  image: { src: string; alt: string; caption?: string };

  // ── كتل م‑٢ ──────────────────────────────────────────────────────────────
  /**
   * شريط شعارات ماركات الأسطول. **كتلة نظامية لا كتلة `items`** (قرار بدر ٤):
   * الشعارات قائمةٌ في `site_settings.fleetBrands` كالخدمات والأسطول تماماً،
   * والكتلة تحمل نصَّيها وحدهما.
   *
   * 🔴 **وتصحيحٌ على المبرر (م‑٧، 2026-08-16):** كان مكتوباً أن السبب بنيوي —
   * «`src` داخل عنصر قائمة شكلٌ لم يُقنَّن في العقد». **الشكل صار مقنَّناً**
   * (‏`lib/item-fields-types.ts` + الهجرة `0065`)، فالسبب اليوم **نطاقٌ لا
   * بنية**: قائمةُ الماركات واحدةٌ للموقع كله، ووضعُها في `items` يجعل ماركةً
   * تُضاف في صفحةٍ وتغيب في أخرى. ونقضُه لا يحتاج إلا حقلين في السجل.
   *
   * 🔴 **وحقلان لا حقلٌ واحد أسفل الشريط — والسبب مقيس (`0072`):** كان التنويه
   * يسكن `note` نفسه، فكُتب فوقه نثرٌ تسويقي واختفى شرط الاستعمال من الصفحة
   * الحيّة بلا خطأ ولا سجلّ. فصار:
   *   `note`       نثرٌ تحريري يملكه المالك — الفارغ يُخفيه.
   *   `disclaimer` شرط الاستعمال (وصفُ طرازاتٍ متاحة عبر المتعهدين، لا رعايةً
   *                ولا شراكةً مع المصنِّعين) — **الفارغ يعيد الافتراضي**، فالمالك
   *                يملك صياغته ولا يملك حذفه.
   */
  "logo-strip": { title?: string; note?: string; disclaimer?: string };

  /**
   * شريط الأرقام. **الكتلة تُشحن والأرقام لا** (قرار بدر ٣): بلا عنصر واحد
   * لا تُصيَّر إطلاقاً، فالمالك يملؤها بما يملك أو يتركها فتغيب.
   *
   * و`value` **نصٌّ يدخل فهرس الترجمة** لا رقمٌ في `style`: العربية تكتب
   * ١٢٬٤٠٠ والإنجليزية 12,400، والفاصلة نفسها تختلف — فرقمٌ غير مترجَم يظهر
   * بصيغة لغةٍ واحدة على اللغتين.
   */
  "stat-band": {
    title?: string;
    items: { value: string; suffix?: string; label?: string }[];
  };

  /**
   * سكة المسارات — **بلا أسعار** (قرار بدر ١): السعر ادّعاءٌ بلا مصدر
   * (`pages` بلا حقل سعر)، والزائر يصل السعر الحقيقي بضغطة على الحاسبة.
   * **وهذا القرار قائم.**
   *
   * 🔴 **أما «بلا صورة» فنُقض صراحةً — 2026-08-16، بأمر بدر:** «ضيف الأعمدة
   * المطلوبة بحيث يمكن التحكم في كل شيء من خلال لوحة التحكم بما فيها الصور
   * والأيكونات». وكان مكتوباً هنا أن الصورة «قيدُ عقدٍ لا سهو» لأن `src` داخل
   * عنصر قائمة شكلٌ مرفوض — **فقُنِّن الشكل** في `lib/item-fields-types.ts`
   * والهجرة `0065`. و`alt` يبقى **نصّاً يُترجَم** ولا يلحق بالوسائط.
   */
  "route-rail": {
    title?: string;
    sub?: string;
    note?: string;
    items: ({
      name: string;
      href?: string;
      duration?: string;
      distance?: string;
    } & ItemMedia)[];
  };

  // ── كتل المستندات (م‑١٠) ─────────────────────────────────────────────────

  /**
   * فهرس الصفحة. **بلا `items` بقصد، وهذا هو قرارها المحوري:** القائمة تُبنى
   * آلياً من كتل `clause` **الظاهرة على الصفحة نفسها** (‏`RenderSections`)، لا
   * من قائمةٍ يكتبها المالك بيده.
   *
   * 🔴 **ولماذا لا `items`؟** لأن فهرساً مكتوباً بيدٍ يعني مصدرين لعنوان البند
   * الواحد: نصّه في البند، ونسخته في الفهرس. وأولُ تعديلٍ على أحدهما يجعل
   * الفهرس يعد بما لا تفي به الصفحة — **وهو انحرافٌ لا يظهر في أي شاشة**
   * (النمط ٤ في `LESSONS.md`). والمالك حين يعيد ترتيب البنود بالسحب لا يخطر
   * له أن يعيد ترتيب فهرسٍ آخر.
   *
   * وثمنه مكتوب: لا يستطيع المالك أن يستثني بنداً من الفهرس ولا أن يسمّيه في
   * الفهرس باسمٍ أقصر. ونقضُه حقلان في السجل يوم يطلبه.
   */
  "page-toc": { title?: string };

  /**
   * بند مرقَّم. `num` **نصٌّ يُترجَم** لا رقمٌ في `style` — «٤» بالعربية و«4»
   * بالإنجليزية، وهو حرفياً مبرّر `stat-band.value`.
   *
   * و`anchor` **معرّفٌ غير نصّي** لا يدخل الفهرس (‏`lib/item-fields-types.ts`
   * §١٠): الغياب يعني مرساةً من معرّف الصفّ، **ولا يُشتقّ من العنوان أبداً** —
   * فتصحيحُ حرفٍ في العنوان كان سيبطل كل رابطٍ أُرسل، صامتاً.
   */
  clause: { num?: string; title: string; body?: string; anchor?: string };

  /**
   * جدول. **أربعة أعمدة سقفاً**، وترويستها حقولٌ عليا (`h1..h4`) وخلاياها
   * حقولٌ داخل العنصر (`c1..c4`) — فكل خليةٍ نصٌّ **مستقلٌّ في فهرس الترجمة**،
   * وهو ما يمنع «جدولاً في حقلٍ واحد بفواصل» من أن يوجد أصلاً: قيمةٌ واحدة
   * تُترجَم ككتلةٍ واحدة تُعيدها لك بأعمدةٍ مبعثرة.
   *
   * والسقف أربعة لأن العقد §٤ يعنون مستويين لا أكثر: العمود الخامس لا يُضاف
   * بسحبةٍ بل بحقلين في السجل — وأغلى ما في العدد المفتوح أنه يوهم بأنه مجاني.
   */
  table: {
    title?: string;
    note?: string;
    h1?: string;
    h2?: string;
    h3?: string;
    h4?: string;
    items: { c1?: string; c2?: string; c3?: string; c4?: string }[];
  };

  /**
   * تنبيه بارز. **والنبرة في `style` لا في النصّ** (العقد §٥): رمزان مغلقان
   * (‏`info`/`warning`) يُختاران من قائمة، فلا يكتب المالك لوناً ولا اسم صنف.
   * ولهذا لا `background` في مقابضها: خلفيةٌ مختارة تحارب تلوين النبرة نفسه.
   */
  callout: { title?: string; body: string };
};

export type Section<T extends SectionType = SectionType> = {
  id: string;
  pageId: string;
  type: T;
  content: SectionContentMap[T];
  sort: number;
  visible: boolean;
  /**
   * الكتلة الأمّ (`sections.parent_id` — هجرة `0058`). `null`/الغياب = كتلة جذر.
   *
   * **اختياري لا مطلوب، والسبب مقيس:** الأقسام الـ٩٣ القائمة كلها بلا أب، ولكل
   * مُحمّل في المشروع مصدرُه (‏`lib/content.ts` للعامة و`app/admin/content/loader.ts`
   * للوحة). جعلُه مطلوباً كان يحوّل كل مُحمّل لم يُحدَّث بعد إلى خطأ بناء بلا أن
   * يكسبه ذلك شيئاً — والعارضة تقرؤه بـ`?? null` فتتصرّف مع الغياب تصرّفها مع
   * الجذر حرفياً.
   */
  parentId?: string | null;
};

export type PageWithSections = Page & { sections: Section[] };

/**
 * الصفحة كما تخرج من طبقة المحتوى العامة (`lib/content.ts`) — الفرق الوحيد عن
 * `PageWithSections` أن ختم آخر تعديل **حقل مطلوب** (وقد تكون قيمته `null` إن
 * لم يصل العمود من القاعدة).
 *
 * لماذا نوع مستقل بدل جعل الحقل مطلوباً في `Page` كلها؟ لأن `updated_at` كان
 * موجوداً في القاعدة منذ `0003` ومع ذلك سقط صامتاً في التحويل، فبقيت خريطة
 * الموقع تعلن تاريخ التصيير لكل صفحة — أي أنها تكذب على محركات البحث بكل بناء.
 * طلبه صراحةً في نوع المخرجات يجعل إسقاطه **خطأ بناء** لا نقصاً صامتاً، دون أن
 * يُلزم مُحمّل اللوحة بحقل لا يستعمله.
 */
export type PublicPage = PageWithSections & { updatedAt: string | null };

/** ثوابت واجهة الإدارة: الاسم العربي + وصف قصير لكل نوع قسم */
export const SECTION_TYPE_LABELS: Record<SectionType, { label: string; hint: string }> = {
  hero: { label: "الواجهة الرئيسية", hint: "ترويسة الصفحة الرئيسية — العنوان والوصف من الهوية أو من هنا" },
  "page-hero": { label: "ترويسة صفحة", hint: "عنوان الصفحة الداخلية ونصها التمهيدي" },
  "services-grid": {
    label: "شبكة الخدمات",
    hint: "الخدمات الست — اتركها بلا عناصر لتُقرأ من بيانات النظام، أو أضف عناصر لتملك كل بطاقة بصورتها وأيقونتها",
  },
  fleet: { label: "الأسطول", hint: "فئات السيارات — بياناتها وصورها من شاشة الأسطول" },
  "why-us": { label: "لماذا نحن", hint: "نقاط القيمة الأساسية" },
  features: { label: "مزايا", hint: "قائمة مزايا بعناصر حرة (عنوان + نص + أيقونة)" },
  "rich-text": { label: "نص", hint: "فقرات نصية — اترك سطراً فارغاً بين الفقرات" },
  faq: { label: "أسئلة شائعة", hint: "أسئلة وأجوبة — تُغذّي نتائج البحث أيضاً" },
  "cta-band": { label: "شريط الحجز", hint: "دعوة للحجز بلون العلامة" },
  contact: { label: "التواصل", hint: "القنوات المفعّلة من الإعدادات" },
  columns: {
    label: "أعمدة",
    hint: "كتلة تخطيط — تحمل حتى أربع كتل أبناء جنباً إلى جنب، ولا نصَّ لها",
  },
  image: { label: "صورة", hint: "صورة من مكتبة الوسائط — النص البديل مطلوب" },
  "logo-strip": {
    label: "شريط الماركات",
    hint: "شعارات ماركات الأسطول — الشعارات نفسها من الإعدادات، وهنا عنوانها وتنويهها",
  },
  "stat-band": {
    label: "شريط أرقام",
    hint: "أرقام تصفك: قيمة ولاحقة وتسمية لكلٍّ — بلا عنصر واحد لا يظهر الشريط",
  },
  "route-rail": {
    label: "سكة المسارات",
    hint: "بطاقات المسارات الشائعة باسمها ومدتها ومسافتها ورابطها وصورتها — بلا أسعار",
  },
  "page-toc": {
    label: "فهرس الصفحة",
    hint: "قائمة روابط لبنود الصفحة — تُبنى وتُرتَّب آلياً من البنود نفسها، فلا تحتاج تحديثاً",
  },
  clause: {
    label: "بند مرقَّم",
    hint: "بند برقمه وعنوانه ونصّه، وله مرساة ثابتة يُرسَل بها رابطٌ مباشر إليه",
  },
  table: {
    label: "جدول",
    hint: "حتى أربعة أعمدة — للنوافذ والرسوم والمدد: ما يُقرأ في ثانيتين ولا يُقرأ في ثلاث فقرات",
  },
  callout: {
    label: "تنبيه بارز",
    hint: "صندوق منبّه بنبرة (معلومة أو تحذير) — للبند الذي يُغضب حين يُكتشف متأخراً",
  },
};
