<div dir="rtl">

# العيوب المفتوحة وحالة البوابة

**آخر تحرير: التحقّق التسلسلي الخامس — 2026-08-18، القياسات بين 04:56 و05:26 UTC (‏07:56–08:26 القاهرة).**
كل رقم هنا مقيسٌ بأمرٍ مذكور وبساعةٍ مذكورة، **ولا شيء منه من تقرير وكيل** — ثلاثة تقارير
سُلّمت قبل هذه الجولة تدّعي إغلاق الحواجز الثلاثة، **وكلُّ ادّعاءٍ فيها أُعيد اشتقاقه من القرص
ومن الكتالوج الحيّ ومن بناء إنتاجٍ بنيتُه بنفسي**. ومن يقرؤه: اقرأ `docs/STANDING-ORDERS.md`
أولاً، ومعه `docs/phase-briefs/OWNER-DECISIONS-2026-08-17.md`.

**ساعةُ الجهاز = ساعةُ القاعدة، والقاهرة = UTC+3** — `date -u` مقابل
`select to_char(now() at time zone 'utc', …)` ⇒ **04:57:37 مقابل 04:57:35**.

🔴 **والشجرة لم تكن ساكنة تحت القياس — وهذا يُقال لا يُخفى.** جبهةٌ رابعة (اتفاقية المتعهد،
`0113`) كانت تكتب أثناء عملي: `app/admin/partner-agreement/page.tsx` ظهر **05:04:16Z** أثناء
بنائي، و`supabase/tests/partner_agreement_tests.sql` ظهر **05:13:09Z** — أي **في الثانية نفسها
التي انتهت فيها جولةُ اختباراتي الأولى**، فلم يرَها المُشغّل. ⇒ **أعدتُ البوابة كلَّها بعد أن
سكنت الشجرة**: آخرُ كتابةٍ **05:14:49Z**، وكلُّ قياسات البوابة أدناه **بدأت 05:19:38** —
و`find -newermt "05:14:50"` ⇒ **لا سطر** طوال الجولة. **فالبوابة أدناه ليست بائتة، والأولى كانت.**

---

## ٠) 🔴 ما زال يسبق كل شيء — الإنجليزية مضاءة للزوار، والحاجز `noindex` وحده

| القياس | الأمر | الساعة | النتيجة |
|---|---|---|---|
| المنشور الإنجليزي | `select count(*) … locale='en' and status='published'` | 04:58:24 · 05:13:44 · 05:26:15 | **٨٧١ — ثلاث مرات بلا حركة** |
| الحالة الكاملة | `select public.translation_progress()` | 05:10 | `(en,891,871,0,20,0,0,97)` ⇒ `published 871` · `draft 20` · **`stale 0`** |
| `/privacy` على بناء إنتاج | `next start :3877` + `curl` | 05:23:42 | **٢٠٠** · `<html lang="ar" dir="rtl">` · ٢٦٦٩١٣ بايت |
| `/en/privacy` | نفسه | 05:23:42 | **٢٠٠** · `<html lang="en" dir="ltr">` · ٢٤٦٦٨٨ بايت |
| تسرّبٌ بين اللغتين | `grep` على الحمولتين | 05:23:5x | **صفر**: لا جملةٌ إنجليزية على `/privacy`، ولا عربيةٌ على `/en/privacy` |

**و`noindex` سليمٌ في المواضع الثلاثة، مقيساً على بناء الإنتاج 05:23:42Z:**

| الموضع | الأمر | النتيجة |
|---|---|---|
| `robots.txt` | `curl :3877/robots.txt` | `User-Agent: *` + **`Disallow: /`** |
| وسم الصفحة العربية | `curl :3877/privacy` | `<meta name="robots" content="noindex, follow"/>` |
| وسم الصفحة الإنجليزية | `curl :3877/en/privacy` | `<meta name="robots" content="noindex, follow"/>` |
| الإعداد في القاعدة | `select value->'robots' from site_settings where key='seo'` | **`indexable = false`** |
| **خريطة الموقع** | `curl :3877/sitemap.xml` | **`<urlset>` فارغ · صفر `<loc>`** |

🔬 **ومتصفحٌ حقيقيّ لا `curl` وحده** (لوح المعاينة، 05:23–05:25Z): `/en/privacy` ⇒
`documentElement.lang="en"` · `dir="ltr"` · الجملةُ الجديدة في عقدةٍ ورقيّة واحدة
(‏`color: rgb(243,241,235)` · `16px` · عرض ٦٣٠px · **بلا تجاوزٍ أفقيّ**)؛ و`/privacy` ⇒
`lang="ar"` · `dir="rtl"` · **`englishLeaks = []`** · بلا تجاوزٍ أفقيّ.
⚠ **ولقطةُ شاشةٍ لم تُلتقط** — لوح المعاينة رفض (`the Browser pane is not displayed`)، وهو
نفسُ الحدّ الذي بلغته الجولات الثلاث قبلي. **فالمظهرُ والتباينُ لم ترهما عينٌ بعد.**

---

## ١) البوابة — مقيسةٌ بيدي، بالأمر والساعة، **بعد أن سكنت الشجرة**

| القياس | الأمر | الساعة (UTC) | النتيجة |
|---|---|---|---|
| **`db:test`** | `node scripts/db-test.mjs > ملف 2>&1` **بلا أنبوب، مرةً واحدة** | 05:19:38 ← 05:21:03 | **خروج ٠ · ٣٩ مجموعة · ٣٩ × `ALL PASSED` · صفر راسبة** |
| حارس التسرّب | داخل الجولة | 05:21:03 | **`🔒 صفر تسرّب — 28 جدولاً مبصوماً من وصلةٍ مستقلّة، ولا رقمَ تحرّك`** |
| **`tsc`** | `npx tsc --noEmit` | 05:21:08 ← 05:21:18 | **خروج ٠ · صفر سطرِ مخرَج** |
| **`next build`** | `npx next build > ملف 2>&1` | 05:21:28 ← 05:21:53 | **خروج ٠** · `Compiled successfully in 4.9s` · **٨٨/٨٨ صفحة** · **١١٥ سطر مسار** (`grep -cE "^[┌├└]"`) |
| **`eslint .`** | `npx eslint . > ملف 2>&1` | 05:21:58 ← 05:22:54 | **خروج ١ — `✖ 4 problems (1 error, 3 warnings)`** · **الخطأ الوحيد `app/admin/set-password/page.tsx:40`** (‏**عيبٌ سابق**) · والتحذيرات الثلاثة في `lib/payments/intents.ts:77` و`scripts/demo-subcontractor.mjs:18` و`scripts/lib/pg-tools.mjs:9` |
| **حارس تسريب العميل** | `node scripts/check-client-value-leaks.mjs` | 05:22:59 | **خروج ٠ · `NO LEAKS FOUND`** · ٤٩٨ ملفاً · ٦١ وحدة `"use client"` · ٣٠٥٨ استيراداً |
| **لم يُكمَّ ولم يُدفَع ولم يُنشَر** | `git status --porcelain` | 05:23 | ✅ · **٠ سطر `D`** · ٣٣ `M` + ٣١ `??` — **ولا واحدٌ منها من كتابتي** |

🔴 **وجولةٌ أولى رسبت، وتُذكر لأن إخفاءها يخدع القارئ التالي:** عند 05:02:54Z رست
`npx tsc --noEmit` بـ`app/portal/agreement/page.tsx(69,14): error TS2344` ثم — بعد بناءٍ
جدّد أنواعَ المسارات — بـ**خمسة أخطاء** في `app/admin/partner-agreement/page.tsx`
و`.next/types/validator.ts` (05:05:00 ← 05:07:06). **السبب لم يكن عملاً في الحواجز الثلاثة**،
بل ملفَّ جبهةٍ رابعة كُتب **أثناء** القياس (05:04:16Z). وبعد سكونها ⇒ **صفر خطأ**.

⚠ **وفخٌّ يُسجَّل: `next build` أخضرُ لا يعني أن الأنواع سليمة.** بناءُ 05:03:19 خرج **٠**
بينما `tsc` كان يرسب على الشجرة نفسها — Next 16 لا يفحص الأنواع داخل البناء، و`next.config.ts`
**بلا `typescript.ignoreBuildErrors`** أصلاً. ⇒ **`tsc --noEmit` هو الحَكَم، لا البناء.**

⚠ **و`.next` موردٌ مشترك**: بناءُ 05:03:19 خرج ٠ ثم **لم يوجد له `BUILD_ID`** حين حاولتُ
تشغيله (‏`Could not find a production build`) — دهسته جبهةٌ أخرى. **أُعيد البناء وشُغّل فوراً.**

---

## ٢) 🔴 نزيفُ تليجرام — ما زال مُغلقاً، مقيساً حول جولةٍ شغّلتُها بنفسي

العيبُ الذي كان يدفع ثمنَه هاتفُ بدر في كل جولة (‏١١ إشعار حجزٍ وهميّ) **ما زال مُغلقاً**،
والقياس حول جولة **05:11:45 ← 05:13:09** بالضبط (`diff` برنامجيّ بين ملفَّي قياس):

| القياس | قبل (05:11:37Z) | بعد (05:13:37Z) |
|---|---|---|
| `select count(*) from notifications` | **١٦١٠** | **١٦١٠** |
| صفوفٌ تذكر هاتف حمزة `6977010427` | **٢** | **٢** |
| صفوفٌ تذكر محادثة الإدارة `1223468133` | **٠** | **٠** |
| صفوفٌ قناتُها تليجرام | **٩٠٨** | **٩٠٨** |
| `max(created_at)` | **02:21:48** | **02:21:48 — لم يتحرّك** |
| صفوفٌ أُنشئت بعد 04:50Z | **٠** | **٠** |
| صفوفٌ سُلّمت بعد 04:50Z | **٠** | **٠** |

> **`diff` بين القياسين ⇒ فارغ. صفرُ إشعارٍ كُتب، وصفرُ رسالةٍ بلغت هاتف بدر من جولتي.**

**وأحدثُ صفٍّ في الجدول كلِّه ما زال 02:21:48Z** — أي من جولة النزيف القديمة قبل الإصلاح،
**ولا شيء بعده**. قارِنه بما كان: `1599 ⇒ 1610` و**١١ رسالة مُسلَّمة** في الجولة الواحدة.

---

## ٣) ما زال مكسوراً — بلا تجميل، مرتَّباً بالخطورة

**خرجت من هذا الجدول ثلاثةُ بنودٍ كانت تحمل الأرقام ١ و٢ و٣ — نصُّ `/en/privacy`، وتصادمُ
`href`، و`NaN` في `quote_public` — وانتقلت إلى القسم ٤ بأدلّتها.** والباقي أُعيد ترقيمه،
**ولم يُحذف منه بندٌ ولم تُخفَّف صياغتُه**. والبندان ١ و٢ أدناه **أعدتُ قياسهما بنفسي
05:25:40Z** فهما ليسا منقولَين.

| # | العيب | الرقم أو الموضع المقيس | الحالة |
|---|---|---|---|
| **١** | 🔴 **حاملُ مفتاح الخدمة يستطيع إطفاء سجلّ التدقيق كلِّه بلا أن يمسّ صفاً واحداً.** `service_role` يحتفظ بصلاحية `TRIGGER` على الجداول الأربعة المحروسة | مقيسٌ حيّاً (03:59–04:00Z): `has_table_privilege('service_role', …, 'TRIGGER')` = **`true`** على `audit_log` · `ledger_entries` · `funnel_events` · `loyalty_entries` — **وأعدتُ القياس 05:25:40Z ⇒ الأربعةُ ما زالت `true`، بلا تغيير**. **وأثبتُّ العاقبة لا استنتجتُها**: بدور `service_role`، `create trigger zz_sv3_kill before insert on audit_log …` ⇒ **نجح**، والمُشغّل ظهر في `pg_trigger` بجوار الحارسين | **مفتوح.** العلاج: `revoke trigger on <الأربعة> from service_role`، وخانةُ `TRIGGER` في مصفوفة `(أ)` من `append_only_tests` (تصير ٤٨ بدل ٣٦). **ولم أنفّذه: بريفي لا يُسنِد لي رقم هجرة، والرقم يُسنَد ولا يُشتقّ** |
| **٢** | 🔴 **`service_role` يمحو قرار الولاء بأمرٍ واحد — بـ`DELETE` لا `TRUNCATE`.** `0110` سحبت `TRUNCATE` وحده بمبرَّرٍ مكتوب «وقرارُ المالك لا يُمحى بأمرٍ واحد» | مقيس: `set local role service_role; delete from loyalty_settings where true` ⇒ **`rowCount=1`**، و`has_table_privilege('service_role','loyalty_settings','DELETE')` ⇒ **`true`** — **وأعدتُ قياسه 05:25:40Z ⇒ ما زال `true`**؛ ولاحظ التباين: `DELETE` على الجداول الأربعة المحروسة = **`false`**، وعلى `loyalty_settings` وحده **`true`**. **والحصيلة متطابقة، والأمرُ واحد.** يخفّفه أن الحذف **مُدقَّق** (‏بفضل `0106`) بينما `TRUNCATE` كان صامتاً. و`append_only_tests` تؤكّد `TRUNCATE = false` **ولا تسأل عن `DELETE` أبداً** | **مفتوح.** العلاج: `revoke delete on loyalty_settings from service_role` + تأكيدٌ في `(أ)`. **ولا طريق عودةٍ من الواجهة إن وقع** (بند ٨) |
| **٣** | 🟠 **ترويسةُ `0110` تدّعي انعدامَ مخرجٍ بمتغيّر جلسة — والمخرج يعمل بدور `DATABASE_URL` نفسه** | مقيس: `set local session_replication_role='replica'` بدور `postgres` ثم `delete from audit_log where id=(select max(id) …)` ⇒ **`rowCount=1`**. سطرٌ واحد يرفع الحرّاس الأربعة، بلا `ALTER TABLE` وبلا أثرٍ في `pg_trigger`. **ويخفّفه أن المتغيّر ممنوعٌ على `service_role`/`authenticated`/`anon`** ⇒ السطح الذي يصله المتصفح أو مفتاح الخدمة سليم | **مفتوح، توثيقيّ**: يُصحَّح نصُّ الترويسة ويُذكر `session_replication_role` صراحةً. **والمجموعة تمسكه سلفاً** |
| **٤** | 🟠 **سقف PostgREST ١٠٠٠ صفّ يمنع مراجعةَ كشفٍ كبير — ولا يجعل الزرَّ يكذب.** `reviews/page.tsx:178` ⇒ `.limit(budget + 1)` وقد تبلغ **١٥٠١** | **السقف مقيسٌ بيدي** (`GET /rest/v1/…?limit=1501` بمفتاح الخدمة، 03:56:56Z): `audit_log` ⇒ **١٠٠٠ صفّاً** · `notifications` ⇒ **١٠٠٠** · `translations` ⇒ **٨٩١** (شاهدٌ أن السقف ١٠٠٠ لا عيبٌ في القياس). **والزرُّ لا يكذب**: `price-sheet-card.tsx:64-65` ⇒ `truncated = shown !== pendingCount` و`decidable = !truncated` ⇒ **فوق ١٠٠٠ صفّ لا يُرسم زرٌّ أصلاً**. والشريط ينصح «أعد تحميل الصفحة» — **ونصيحةٌ لا تنفع أبداً**، لأن الحاجز إعدادُ خادم | **مفتوح، تشغيليّ.** وامتدادُه: `app/portal/prices/_lib/sheets.ts:130` — `countItemsByRoute` تنادي `.in(price_list_id, …)` **بلا سقف**، و`loadRoutes` (‏`:107`) **بلا `.limit()`** ⇒ عند تجاوز ١٠٠٠ بند **يبدأ عدّاد «الفئات المسعَّرة» في بوابة المتعهد بالكذب صامتاً**. **وغير قابلٍ للوقوع اليوم: مساران في القاعدة** |
| **٥** | 🟠 **حارسُ «عمود فئة مكرّر» في `0109` يحرس مساراً لا يسلكه المتعهد.** `app/portal/prices/_lib/csv.ts:137-150` يكتب الفئات في كائن JavaScript بمفتاح `rawName.trim().toLowerCase()` | **أعدتُ تشغيل الحلقة حرفياً في Node**: ترويسة `suv · SUV · minibus` بقيم `800 · 1200 · 2000` ⇒ `prices = {"suv":"1200","minibus":"2000"}` — **٨٠٠ اختفت** — و`unknown` **مجموعةٌ (`Set`) فلا أثر للتكرار**. ⇒ العمودان ينهاران في المتصفح **قبل** أن تراهما Postgres، والحارس الجديد لا يُنادى إلا من نداء RPC مصنوعٍ باليد | **مفتوح.** والمسار الذي يسلكه المتعهد فعلاً ما زال يبتلع عموداً بلا كلمة، **والأخير يفوز** |
| **٦** | 🟠 **المتعهد يقرأ «فشل الحفظ» عامّاً مهما كان السبب.** `app/portal/prices/actions.ts` يترجم كل خطأ RPC إلى `error=save` (‏`:211 · :218 · :223 · :229 · :306 · :309 · :331 · :349`) | فرفضُ `price_list_items_cost_finite_chk` أو أيُّ رسالةٍ عربيةٍ صريحةٍ من القاعدة **لا تصل الشاشة**. **وهي شاشة عميلك التجاري لا شاشتك** | **مفتوح** |
| **٧** | 🟠 **تنبيه «الفئة الساقطة» صوتٌ خافت.** النصّ صحيح ويسمّي الفئة، لكنه في `import-panel.tsx` يقع في عمود «السبب» على صفٍّ حالته «أُضيف»، بأرضيةٍ عادية، **ولا تعدّه شارةُ الملخّص** (`مقبول ن` · `مرفوض ن` فقط) | في ملفٍ من مئة صفّ يمرّ بالتمرير | **مفتوح، تجميليّ** |
| **٨** | 🟠 **فرعُ `insert` في `saveLoyaltySettings` ميت.** `relacl` لـ`loyalty_settings` = `authenticated=rw` (‏`SELECT`+`UPDATE` فقط) ولا سياسة `INSERT` | ⇒ `app/admin/loyalty/actions.ts:132` **يُخرج «فشل الحفظ» دائماً** لو غاب الصفّ. عيبٌ سابق | **مفتوح** — ويعني أن **البند ٢ بلا طريق عودةٍ من الواجهة** |
| **٩** | 🟠 **`0108` لقطةٌ لا قاعدة.** تمشي على أعمدة `numeric` **الموجودة لحظة تنفيذها** | **واليوم لا تكلّف شيئاً، مقيساً** (04:00Z): أعمدة `numeric` في `public` = **٦٣** · قيود `finite_chk` = **٦٣** · **صفر عمودٍ بلا قيد** · **صفر قيدٍ `not valid`** · وأعمدة `float4`/`float8` = **٠** (وهي التي تقبل `NaN` بطبعها ولا يمرّ بها المسح). لكن **عموداً رقمياً يولد غداً في هجرةٍ جديدة يولد بلا حارس**، والشيء الوحيد الذي يمسكه هو `(ك-٤)` — أي **إنسانٌ يشغّل `db:test`** | **مفتوح، قرارُ بدر**: `event trigger` على `ddl_command_end` يحوّلها من لقطةٍ إلى قاعدة |
| **١٠** | 🟠 **٢٣٬٣٨٣ صفَّ فيكسترة مجمَّدةً في `audit_log` سنةً كاملة** | مقيسٌ 04:07Z: `audit_log` = **١١٨٥٧٣** · **أصغرُ من ٣٦٥ يوماً = ١١٨٥٧٣ (كلُّها)** ⇒ خارج مخرج التقليم تماماً · **وتحمل وسمَ اختبار (`TEST` أو `FIXTURE` أو `PROBE` أو `ZZ_` أو `DTEST` أو `REVIEW-`) = ٢٣٣٨٣ (٢٠٪)**. و`0110` أقفلتها بيدها | **مفتوح، قرارُ بدر**: هجرةُ تنظيفٍ بـ`DISABLE/ENABLE TRIGGER` مقصودة، أو الانتظار سنة. **لا ثقبٌ أمني، لكنه فعلٌ لا رجعةَ فيه** |
| **١١** | 🟠 **`review_price_sheet` تحرس عدداً لا محتوى.** `p_expected` **عدد**، والعدد هو الخاصية الأسهل على المتعهد أن يحافظ عليها | قرأتُ الجسم من `pg_get_functiondef`: تلتقط `for update` **لحظة الضغط** وتكتب على `id = any(v_ids)` — على الحاضر لا على المعروض. **والمُمكِّن أسبقُ من `0109`**: `import_price_sheet_rows` ترفض المسار المعتمد والمنتظر برسالة صريحة، بينما **`upsert_price_list` تسمح للمتعهد بتعديل مسارٍ على مكتب المشرف**، وRLS تسمح له بحذفه ⇒ تعديلٌ يحفظ العدد ويقلب التكلفة ×١٠٠ يمرّ بلا اعتراض | **مفتوح.** أرخصُ علاجٍ يغلق نصف الباب فوراً: **منع `upsert_price_list` والحذف على صفٍّ `pending`** (كما فعل المستورد بالضبط). والأتمّ: بصمةُ `md5` على المحتوى بدل العدد |
| **١٢** | 🟠 **`--tone-warning` `#E17100` يرسب `1.4.11` على الأرضية الفاتحة**: **2.84** أرضية · 3.01 بطاقة. وأصغرُ تصحيحٍ يعبر: `#E17100 ⇒ #DA6B00` | (قياسٌ سابق من الجولة الثالثة — **لم أُعِده هذه الجولة**) | **مفتوح — قرارُ منتجٍ يخصّ بدر.** كلمةٌ واحدة تكفي |
| **١٣** | 🟠 **`text-destructive` فوق `bg-destructive/20`**: **4.18** فاتحاً · **4.21** داكناً أمام 4.5. والعلاج الوحيد عمقُ التِنت `/20 ⇒ /14` (‏4.64 · 4.63) | الجذر في `components/ui/button.tsx` و`badge.tsx` (‏٤ مواضع). (قياسٌ سابق) | **مفتوح** — خارج ملفّات كل من عمل الليلة |
| **١٤** | 🟡 **حارس `check-export-status-parity.mjs` أعمى على المحور الذي وقع عليه العيب**، و`parseConstraint` يُسقط أي معرّفٍ فيه رقم (`refund_v2`) صامتاً | الحدُّ مُعلَنٌ في ترويسته صراحةً | **مفتوح، منخفض** |
| **١٥** | 🟡 **`assigned` بلا تبويبٍ في `/admin/orders`** — و**٣ من ١٧ حجزاً `assigned`، وهي وحدها الحاملة `subcontractor_cost`/`margin_amount`** | التأجيل مكتوبٌ بقصد في `page.tsx:44` | **مفتوح بقرار** |
| **١٦** | 🟡 **الخطُّ الصلب داخل `transition-colors`** (‏0.16s) ⇒ أولُ ~١٦٠ مللي عند القيمة الراسبة، **وفي تبويبٍ مخفيّ لا يتقدّم الانتقال إطلاقاً** | (قياسٌ سابق) | **مفتوح، تجميليّ** |
| **١٧** | 🟡 **القراءةُ الخاطئة التي أسقطت `0104` ما زالت مكتوبةً في ملفَّي عقود، بلا أثرٍ تنفيذيّ اليوم.** `lib/item-fields-types.ts:247-264` يقول عن `href`: «نعم — بلا تغيير … فالوجهة تختلف بالفعل باختلاف اللغة»، ومعه فقرةُ «الحجّة المضادة وردّها» التي تُبقيه مفهرَساً؛ و`lib/page-builder-types.ts:1069-1072` يكرّر الجملة على تعريف `route-rail` | **الحجّة خاطئةٌ مقيساً** (‏القسم ٤/ط): D‑24 لا يقولها، و`pages` بلا عمود لغة. **ولا انحرافَ بين TS والقاعدة اليوم** — `TRANSLATABLE_COMPANION_FIELDS` **غير مستعمَلٍ في أي ملف** (‏`grep` عبر المستودع ⇒ إعلانُه فقط) | **مفتوح، توثيقيّ — وخطرُه أنه يُقنع القارئ التالي بنقض `0104`.** ⚠ **وما يجب ألّا «يُصلَح»**: بقاءُ `href` في `item_fields` داخل `block_registry` **صحيحٌ ومقصود** — كتالوجُ المنشئ يقرّر «بأيّ مُدخلٍ يحرّره المالك»، والحارس يقرّر «هل يُترجَم». **محوران مختلفان** |
| **١٨** | ⚠ `next build` يطبع تحذير تجاهُل `package-lock.json` في `C:\Users\Badr` لأنه خارج المستودع | **سطر ٣ من سجلّي 04:04:46Z** — ما زال | **قائم، غير مؤثّر** |
| **١٩** | ⚠ خادم dev على `:3000` يعطّل الفحص ⇒ الأداء والتوقيت يُقاسان على بناء الإنتاج | وهو ما فعلتُه (‏منفذ 3852) | **دَينٌ قائم** |
| **٢٠** | ⚠ **مجلد المسوّدات مشترَكٌ بين الوكلاء المتوازين، والكنسُ يجري بينما أحدهم يقيس** | وقع مرتين ليلتَه بحسب تقريرَين مستقلَّين: ملفُّ بصمةٍ باسمٍ عامّ **دُهس صامتاً**، وأربعةُ ملفّات مراجعٍ **اختفت بين 03:38 و03:40Z وهو يشغّل**. **ولم يقع عندي** لأن بادئتي فريدة (`_sv3_`) | **خطرٌ تشغيليّ مفتوح**: مَن يقيس انحرافاً من ملفٍّ باسمٍ عامّ قد يقارن شيئاً ليس شيئه |

---

## ٤) ما أُغلق فعلاً هذه الجولة — قِستُه بيدي، لا من تقرير

### أ) ✅ `NaN` في المال (‏`0108`) — أُغلق، والحاجزُ عند **العمود** لا في جسم الدالة

بهوية مشرفٍ حقيقية (`set local role authenticated` + `request.jwt.claim.sub = 7aa6106a…`
⇒ `is_admin() = true`) داخل معاملةٍ **يفرض المُشغّل إرجاعَها في `finally`**:

| النداء | النتيجة المقيسة (03:54–03:55Z) |
|---|---|
| `convert_quote_request(…, 'NaN', …)` | **`P0001 · hint=cost-not-finite`** — «أساس التكلفة ليس رقماً حقيقياً (NaN)» |
| نفسه بـ`'Infinity'` | **`P0001 · cost-not-finite`** |
| **`convert_quote_request(…, 1000, …)` على `RQ-ZF83NH` الحقيقي** | ✔ **حجزٌ `TR-FJ7BC5`** · `total=8500.00` · `cost=1000.00` · `margin=7500.00` · `pending_payment` · `price_source=subcontractor` — **التحويل المشروع ما زال يعمل** |
| `set_quote_request_status(…,'NaN')` | `23514 · quote_requests_quoted_amount_finite_chk` |
| `upsert_price_list(… '[{"cost":"NaN"}]')` | `23514 · price_list_items_cost_finite_chk` |
| `UPDATE bookings SET margin_amount='NaN'` بدور المتصفح | `23514 · bookings_margin_amount_finite_chk` |
| نفسه على `total` · `subcontractor_cost` | `bookings_total_finite_chk` · `bookings_subcontractor_cost_finite_chk` |
| `INSERT INTO price_list_items … 'NaN'` | `23514 · price_list_items_cost_finite_chk` |

**وثلاثةُ مسالكَ لم يذكرها بنّاءٌ ولا مراجع، فحصتُها بشروطها الصحيحة (03:55Z):**

| المسلك | `NaN` | `Infinity` | قيمةٌ مشروعة |
|---|---|---|---|
| `manual_assign` على حجزٍ `confirmed` | **`23514 · trip_offers_payout_finite_chk`** (‏لا `dispatches.assigned_payout` كما قال التقرير) | `22003` | ✔ `payout=500` |
| `mark_booking_failed(… 'driver-no-show','deduct', …)` | **`23514 · ledger_entries_amount_finite_chk`** | `22003` | ✔ `deduct=50.00` |
| `redeem_points` بدور المتصفح | **`42501 permission denied`** — غير ممنوحة لـ`authenticated` أصلاً | — | — |

**والتغطيةُ كاملةٌ للنوع** (04:00Z): `numeric` = **٦٣** · `finite_chk` = **٦٣** · `not validated` = **٠** ·
`float4`/`float8` = **٠**. **والثقب الذي كان باقياً — `quote_public` — أُغلق بـ`0112`، وبرهانُه في (ك) أدناه.**

### ب) ✅ صدقُ زرّ المراجعة (‏`0109`) — أُغلق، وبرهانٌ على كشفٍ أكبر من السقف

بنيتُ كشفاً بـ**١٢٠٠ مسارٍ منتظر** داخل معاملةٍ مُرجَعة (03:57Z):

| الخطوة | المقيس |
|---|---|
| `price_sheet_stats(null)` | `pending_count = **1200**` |
| `review_price_sheet(sheet, true, null, **1000**)` — أي ما يعرضه سقف PostgREST | **`P0001 · hint=count-changed`**: «عُرض عليك 1000 مساراً والكشف يحمل الآن 1200 — لم يُكتب شيء» |
| الحصيلة بعده | **`approved = 0` · `pending = 1200`** — **لم تُكتب خانةٌ واحدة** |
| `review_price_sheet(sheet, true, null, **1200**)` | **`affected = 1200`** · `approved = 1200` |

⇒ **الرقم على الزرّ يساوي الرقم الذي تكتبه الدالة، أو لا يُكتب شيء.** والدالةُ **تُلزِم**
`p_expected` (‏`null` ⇒ `invalid-input`) — فليست مصادفةً تتطابق اليوم.

### ج) ✅ `bidirectional` — **العاقبة مقيسة لا البوليان**

استيرادٌ حقيقيّ عبر `import_price_sheet_rows(p_commit=true)` بأربعة أشكال، ثم `quote_price`
على شقّ العودة (03:59Z):

| الشكل في الملف | المخزَّن | **سعرُ العودة** |
|---|---|---|
| `"bidirectional": ""` (خانةٌ فارغة) | `true` | **١٢٠٠ · `price_source=subcontractor` · تكلفة ١٠٠٠** |
| العمود **غائبٌ كلياً** | `true` | **١٢٠٠ · `subcontractor`** |
| `"bidirectional": "false"` | `false` | **٨٦٠ · `tariff`** (‏والذهاب ١٢٠٠ `subcontractor`) ✔ المقصود |
| `"bidirectional": "maybe"` | — | **الصفُّ مرفوضٌ برسالةٍ تسمّي القيمة**: «قيمة عمود الاتجاهين «maybe» غير مفهومة…» |

**والعيب ٧ القديم (‏خليةٌ فارغة تقلب المسار إلى اتجاهٍ واحد صامتاً) أُغلق** — و`import_field`
هو الجذر: `nullif(btrim(coalesce(…,'')),'')` ⇒ **الفراغ = الغياب في كل حقل**.

**والكشفُ غير المعتمد لا يُسعّر شيئاً، مقيساً**: `draft` ⇒ **١٠٨٠ `tariff`** · و**`pending` كذلك
⇒ ١٠٨٠ `tariff`** · ولا تغطية إلا بعد `approved`.

**و`NaN` في المستورد مسدود** (03:59Z): `prices.suv = "NaN"` ⇒ «تكلفة غير صالحة في: suv» ·
`"Infinity"` ⇒ نفسها · `originLat = "NaN"` ⇒ «إحداثيات البداية خارج مصر». **والحدود ذات
طرفين، فـ`NaN > 10000000` صادقة وتسقطه.**

### د) ✅ سجلٌّ مُلحَقٌ فقط أمام كل دورٍ يصله المتصفح أو مفتاح الخدمة (‏`0110`)

ثمانيةُ هجماتٍ حيّة داخل معاملةٍ مُرجَعة (03:59–04:00Z)، **كلُّها `42501`**:

| الفعل | `service_role` | `authenticated` (بهوية مشرف) |
|---|---|---|
| `UPDATE audit_log` | `42501` | `42501` |
| `DELETE FROM audit_log` | `42501` | `42501` |
| `TRUNCATE audit_log` | `42501` | `42501` |
| `ALTER TABLE … DISABLE TRIGGER ALL` | `42501 must be owner` | `42501 must be owner` |
| `DROP TRIGGER` | `42501 must be owner` | — |
| `DELETE ledger_entries` · `UPDATE funnel_events` · `DELETE loyalty_entries` | `42501` × ٣ | — |

**وبدور المالك الحارسُ حيّ**: `DELETE` ⇒ `P0001 hint=append-only` «لا يُحذف قبل انقضاء 365 days
— والتقليم وحده يمرّ» · `UPDATE` ⇒ `P0001` · `TRUNCATE` ⇒ `P0001` «وRLS لا تغطّي TRUNCATE،
فالمنعُ مُشغّلٌ لا سياسة».

**والكتابةُ المشروعة ما زالت تُدقَّق**: `update loyalty_settings set min_redeem_points = min_redeem_points + 1`
بدور `service_role` ⇒ `rowCount=1` و**`audit_log` ١١٨٥٧٣ ⇒ ١١٨٥٧٤** بصفٍّ
`{loyalty_settings, update, system, {"min_redeem_points":{"from":1000,"to":1001}}}`.
⚠ **ومصيدةٌ تُذكر**: تحديثٌ **بلا تغيير قيمة** لا يكتب صفَّ تدقيق (‏`log_audit` تتخطّى المتطابق)
— فقياسٌ بقيمةٍ ثابتة يُعطي «+0» ويُقرأ خطأً «التدقيق مكسور».

### هـ) ✅ `db:test` لم يعد يكتب في `locales` — العيب ١٣ القديم أُغلق

الجولاتُ السابقة كانت تحرّك `locales.en.updated_at` و`discount_settings.updated_at`
و`pricing_settings.updated_at` و`loyalty_entries` في كل مرة. **وجولتي حرّكت صفراً**:
الأربعة **بنفس طوابعها بالضبط** قبل الجولة وبعدها (‏`02:21:27` · `02:21:48` · `02:21:59`)،
و`loyalty_entries` **١١٤ ⇐ ١١٤**.

### و) ✅ بايت `NUL` في `languages-ui.tsx` — أُغلق

`node` قرأ الملف بايتاً ببايت: **الحجم ٢٣٧٠٣ · عددُ بايتات `NUL` = ٠**.
و`grep -n "rowIdentity"` صار يجده نصّاً في **السطر ٤١٣** (‏لا «Binary file … matches»)،
و`MAX_QUEUE_ROWS = 150` في `:35`. **والهوية مطابقةٌ بايتاً ببايت**:
`settings\u0000brand.name` ⇒ `73657474696e6773006272616e642e6e616d65` — **نفسُ السداسي
عشري** لبايت `NUL` الخام.

### ز) ✅ المفاتيح الأربعة عشر (‏`0103`) و`href` (‏`0104`) — نزلتا ومقيستان

- **١٤/١٤ مفتاحاً تحلّ إلى صفٍّ حيٍّ في الفهرس** (`i18n_corpus_rows()`): ٦ في `settings` و٨ في `vehicle`، **كلُّها `draft` بـ`provider='human'`** — ولا حرف `published`.
- `translation_progress()` ⇒ `total 891 · published **871** · draft 20 · **missing 0** · stale 1 · 97%`.
- `i18n_non_text_field('href')` ⇒ **`true`** · **الفهرس ٨٨٥ صفّاً** · و**صفر صفٍّ ينتهي بـ`.href` فيه** · وصفوفُ `href` الستّة في `translations` **كلُّها `draft` من `mymemory`** ⇒ **لا صفَّ منشورٍ أُيتّم** (‏`published_not_in_corpus = 0`).
- ⚠ **و`<title>` على `/en` ما زال عربياً** («ايجار ليموزين») لأن `settings.brand.name` مسودة — **مقصودٌ لا عطل**، ويُحسم بزرّ «راجِع وانشر».

### ح) ✅ نصُّ الخصوصية العربيّ — أُصلح ومقيسٌ حيّاً على بناء إنتاج

`curl :3852/privacy` (04:06Z) يعرض حرفياً: «…اسمك ورقم هاتفك **ورقم واتسابك إن سجّلته**،
ونقطتا الانطلاق والوجهة **بعنوانيهما الكاملين**، وموعد التحرك وعدد الركاب وفئة المركبة
ومسافة المسار وساعات الانتظار، **ورقم رحلتك الجوية إن أدخلته**، **وملاحظاتك على الحجز كما
كتبتها بلا حذف**. ولا يصلهما إيصال التحويل ولا السعر الذي دفعته، **ولا أيُّ رحلةٍ من رحلاتك
غير التي أُسند إليهما تنفيذها.»

**وقابلتُه بـ`portal_trips()` من `pg_get_functiondef` حقلاً بحقل** ⇒ مطابق.
⚠ **وبقي حقلان تُخرجهما الدالة ولا يسمّيهما النصّ**: `round_trip` (‏ذهاباً وعودة) و`status`
(‏حالة الحجز) — **صفاتُ رحلةٍ لا بياناتٌ شخصية زائدة**، لكن النصّ يعدّ عدّاً يبدو حاصراً.
**و`reference` = `partner_trip_code()` لا `TR-…`** ⇒ D‑46 صامد.

---

### ط) ✅ تصادمُ `href` — أُغلق، والتأكيدُ هو الذي قُلب لا الهجرة (‏العيبُ ٢ سابقاً)

**الحُكم: `0104` صواب، وملفُّ الاختبار كان البائت.** والسببُ لم أنقله عن أحد، بل قِستُه:

| القياس | الأمر | النتيجة |
|---|---|---|
| الحالة الحيّة (D‑58 — من الكتالوج لا من ملف) | `select public.i18n_non_text_field('href')` | **`true`** · و`alt`/`title` ⇒ **`false`** (‏الاتجاه الآخر سليم) |
| صفوف `href` في الفهرس | `select … from translations where key like '%.href'` | **٦ صفوف · كلُّها `draft` · منشور = ٠** |
| المجموعة منفردةً | `node scripts/db-test.mjs page_builder` | **05:00Z ⇒ خروج ٠ · `ALL PASSED` · `🔒 صفر تسرّب`** |
| المجموعة داخل الجولة الكاملة | `node scripts/db-test.mjs` | **05:21:03 ⇒ ضمن ٣٩/٣٩** |

**والتبرير المكتوب في الملفّ صادقٌ — فحصتُ كل دعوى فيه بأمرها:**

| الدعوى في التعليق | ما قِستُه |
|---|---|
| «D‑24 لا يذكر اختلافَ الوجهة» | قرأتُ `handover/DECISIONS.md:243-247` كاملاً: «العربية افتراضية وبلا بادئة؛ `/en/...` **إعادة كتابة داخلية**» — **ولا ذكر لاختلاف وجهة**. ✅ |
| «البادئة يضيفها المصيِّر بعد `i18n_apply`» | الكتلُ الثلاث تنادي `localeHref`: `components/sections/route-rail.tsx:197` · **`components/site/services.tsx:113`** · `components/sections/logo-strip.tsx:165`. ✅ (والتعليق يكتبها `services.tsx:113` — **الملفّ في `components/site/` لا `components/sections/`**، وهو الموضع الوحيد الذي يحتاج توضيحاً) |
| «`pages` بلا بُعدٍ لغويّ» | **٢٣ صفّاً · ٢٣ `slug` متمايزاً · صفرُ عمودٍ `%locale%`** ⇒ لا «صفحةٌ إنجليزية أخرى» يشير إليها رابطٌ مترجَم. ✅ |
| «الثمن مقيسٌ: `cairo-lexandria`» | الصفُّ `…0003.items.rtalex.href` قيمتُه **`/routes/cairo-lexandria`**، و`pages` تحمل **`cairo-alexandria`** ⇒ **رابطٌ ميت لو نُشر**. ✅ **شاهدٌ حقيقيّ لا مثال** |
| «`internalPath` ترفض الخارجيّ» | `components/site/links.ts:202-207` ⇒ ما لا يبدأ بـ`/` (أو يبدأ بـ`//`) ⇒ `null`. ✅ |
| «الحارس السلوكيّ لا يُستنسخ، بل يُحال إليه» | `supabase/tests/i18n_href_tests.sql:187` يمرّر `/routes/cairo-lexandria` في القسم (ج) ويثبت أن `i18n_apply` تُبقي الأصل. ✅ **مجموعةٌ شقيقةٌ خضراء، فلا تصادمَ بقي داخل شجرة الاختبارات** |

⚠ **وما لم يُصلَح ويجب أن يُعرف:** القراءةُ الخاطئة نفسها **ما زالت مكتوبةً في ملفَّي TypeScript**
(`lib/item-fields-types.ts:247-264` و`lib/page-builder-types.ts:1069-1072`: «فالوجهة تختلف بالفعل
باختلاف اللغة»). **بلا أثرٍ تنفيذيّ اليوم** — لكنها ستُضلّل أول من يقرأها. **وهو البند ١٧ في الجدول أعلاه.**

### ي) ✅ `/en/privacy` — أُغلق، والنصُّ يطابق ما تُخرجه `portal_trips()` حرفاً بحرف (‏العيبُ ١ سابقاً)

الصفُّ `…3303.items.pvw002.c2` **منشورٌ** (`provider=human` · `updated_at = 2026-08-18 04:38:06Z`)
ويقول الآن:

> "…After acceptance they receive **what carrying it out requires**: your name, your phone number
> **and your WhatsApp number if you gave one**, the pickup point and the destination **with their
> full addresses**, the departure time, the number of passengers, **the vehicle class, the route
> distance and the waiting hours**, **your flight number if you entered one**, and **your booking
> notes exactly as you wrote them, with nothing removed.** They do not receive the transfer receipt,
> **nor the price you paid**, nor any trip of yours other than the one assigned to them."

**والمطابقةُ عدّدتُها بنفسي من `pg_get_functiondef` (D‑58) لا من تقرير.** `portal_trips()` تُخرج
**٢٥ عموداً** (لا ٢٤ كما قال التقرير)، وكلُّ حقلٍ شخصيّ فيها مذكورٌ في **اللغتين**:
`customer_name` · `customer_phone` · **`customer_whatsapp`** · `origin_label` · `dest_label` ·
`pickup_at` · `passengers` · `class_title` · `distance_km` · `waiting_hours` · **`flight_number`** ·
و**`notes` خاماً** (`b.trip ->> 'notes'` بلا `dispatch_safe_notes`) — ومنفيٌّ فيها إيصالُ التحويل
وسعرُ العميل والرحلاتُ الأخرى.

🔬 **والتمييز «قبل القبول / بعده» ليس بلاغةً — قِستُه:** `portal_offers()` (‏ما قبل القبول)
تُخرج `notes` **عبر `public.dispatch_safe_notes(...)`** (سطر ٢٢ من جسمها)، بينما `portal_trips()`
(‏بعد القبول) تُخرجها **خاماً**. **فجملةُ «لا تصلهما بياناتك قبل قبول المتعهد» صادقةٌ بنيوياً.**

| القياس | الساعة | النتيجة |
|---|---|---|
| على بناء إنتاج | 05:23:42Z (‏بناء 05:21:53Z) | الجملةُ الإنجليزية **حاضرة** · العربية **حاضرة** · **صفر تسرّبٍ متبادل** |
| العربية لم تُمسّ | `sections` md5 | **`705d2f8499c39857f25af6a18870f09a` — متطابقٌ في القياسات الثلاث** |
| المنشور الإنجليزي | ثلاث قياسات | **٨٧١ · ٨٧١ · ٨٧١** — والصفُّ لم يُقلب `draft` |
| `translation_progress().stale` | 05:10Z | **٠** (كان ١) |

⚠ **وحقلان تُخرجهما الدالة ولا يسمّيهما النصّان: `round_trip` و`status`.** **والعربيةُ لا تسمّيهما
كذلك** ⇒ **لا انحرافَ بين اللغتين**، وهما صفتا رحلةٍ لا بياناتٌ شخصية. **قرارٌ واحدٌ لبدر
يُطبَّق على النصّين معاً إن أراد ذكرهما.**

### ك) ✅ `quote_public` لم تعد تُخرج ما ليس رقماً لزائرٍ من الشارع (‏العيبُ ٣ سابقاً)

**مقيسٌ بدور `anon` الحقيقي داخل معاملةٍ مُرجَعة (05:01–05:02Z):**

| المُدخَل | المسار | قبل `0112` (من الجولة الرابعة) | **الآن** |
|---|---|---|---|
| `p_waiting_hours = 'NaN'` | مغطّى | `total = "NaN"` للفئتين | **`P0001` · `hint=invalid-input` · «ساعات الانتظار» ليست رقماً صالحاً** |
| `'Infinity'` · `'-Infinity'` | مغطّى | `total = "Infinity"` | **مرفوض** |
| `'1e1000'` · `'-1e1000'` | مغطّى | إجمالي بـ١٠٠١ خانة | **مرفوض — «خارج المدى المقبول (‏±100000)»** |
| `p_distance_km = 'NaN'` | **تعريفة** | `"NaN"` | **مرفوض** |
| الإحداثيات `'NaN'`/`'Infinity'`/`'1e1000'` | — | إسقاطُ تغطيةٍ صامت / `22003` خام | **مرفوضة — و±180 للإحداثيات** |

**والحاجزُ في الجذر لا في الواجهة، وأثبتُّ التفويض لا افترضتُه:** `position('quote_arg_finite' in
pg_get_functiondef(...))` ⇒ **`quote_price/9` = `true`** وحدها؛ **و`quote_price/4` و`quote_price/5`
تُرجعان الخطأ نفسه** حين جُرِّبتا مباشرةً ⇒ **التفويض حقيقيّ، وموضعٌ واحد يغلق كل الأبواب**.
و`quote_price` **غيرُ ممنوحةٍ لـ`anon` أصلاً** (‏`42501`) ⇒ البابُ الوحيد من الشارع هو `quote_public`.

**ولا سعرَ زُحزح — والحدُّ مقيسٌ على حافّته:**

| القياس | النتيجة |
|---|---|
| **أرقام بدر الستّة، بدور `anon`** | **٧٢٠/١٥٠٠ · ١٨٠٠/٢٦٤٠ · ٣٢٥٠/٤٩٥٢** — مطابقةٌ تماماً |
| الحافّة | `100000` **تمرّ** (إجمالي ١٬٦٠٠٬٣٠٠) · `100001` **تُرفض** |
| الانتظار المشروع | ٢٤ ساعة ⇒ **٧١٥٢** يمرّ |
| القصُّ القديم لم يُمسّ | `quote_public(-10,4,false,-5,…)` ⇒ **١٥٠٠** كما كان |

---

## ٥) الهجرات — القرصُ مقابل الدفتر (05:23:10Z)

| القياس | الأمر | النتيجة |
|---|---|---|
| ملفّات `supabase/migrations/*.sql` | `ls … \| wc -l` | **١١١** |
| صفوف `schema_migrations` | `select count(*)` | **١١٢** |
| `on_disk_not_applied` | `diff` برنامجيّ بين القائمتين | **`[]`** — صفر معلَّق |
| `in_db_not_on_disk` | نفسه | **`["0100_logo_strip_list_label.sql"]`** — **اليتيم المعروف الموثَّق ⇒ لا يُمسّ ولا يُحذف ولا يُعاد ترقيم شيء** |
| أرقامٌ مكرَّرة على القرص | `cut -c1-4 \| sort \| uniq -d` | **لا سطر** — **لا تصادمَ جديد** |
| فجوات الترقيم | — | **`0090` · `0091` فقط — كما كانت** |
| أعلى رقم | — | `0113_partner_agreement.sql` |
| **الرقم الحرّ التالي** | — | **`0114`** |

**والثلاثةُ التي نزلت هذه الليلة بعد آخر تحرير:** `0111_privacy_en_matches_what_partner_receives.sql`
· `0112_public_quote_refuses_non_numbers.sql` · `0113_partner_agreement.sql` — **وكلُّها مطبَّقة،
وصفرُ ملفٍّ معلَّق.** ولم أطبّق أنا شيئاً: **بريفي بلا رقمٍ مُسنَد، والرقم يُسنَد ولا يُشتقّ.**

---

## ٦) بيانات بدر — **محورٌ واحدٌ تحرّك، وليس من عملي، وأقوله بدل أن أدّعي صفراً**

بصمةُ **٢٧ محوراً** (‏عدّادات + `md5` صفوف)، ثلاث مرات: **04:58:24Z** (قبل أي شيء) ·
**05:13:44Z** (بعد جولة الاختبارات) · **05:26:15Z** (بعد كل شيء)، ثم `diff` برنامجيّ.

> ### **04:58 ⇄ 05:13 ⇒ `diff` فارغ · SAME = 27 · DIFF = 0**
> ### **04:58 ⇄ 05:26 ⇒ SAME = 26 · DIFF = 1**

🔴 **والمحورُ الوحيد الذي تحرّك: `audit_log` ١١٨٥٧٥ ⇐ ١١٨٥٧٧ (‏+٢).** ولم أخمّن السبب —
عدّدتُ الصفّين: `select … from audit_log where occurred_at > '04:58:24Z'` ⇒

| الساعة | الفاعل | الفعل | الكيان |
|---|---|---|---|
| 05:21:39Z | `admin` | `insert` | `partner_agreement_versions` |
| 05:22:31Z | `admin` | `delete` | `partner_agreement_versions` |

⇒ **تحقّقٌ حيٌّ أجرته جبهةُ اتفاقية المتعهد بهوية مشرف، أدرجت نسخةً ثم حذفتها.** **ليس من عملي**
(‏لم أنفّذ إلا استعلامات قراءة، وجولاتِ اختبارٍ يُرجعها المُشغّل)، **وبلا متساقطات**:
`partner_agreement_versions` = **صفٌّ واحد**، وهو المزروع بالهجرة 04:54:15Z. **و`audit_log`
مُلحَقٌ فقط بحكم `0110` فلا سبيل لطيّ الصفّين، ولا داعي — هما أثرُ عملٍ مشروع.**

**وكلُّ محورٍ يخصّ المالك لم يتحرّك، في القياسات الثلاث:**

| | القيمة، ثابتةً في القياسات الثلاث |
|---|---|
| الحجوزات | **١٧** · `md5` المرجع/الحالة/`total`/`amount_due`/`amount_remaining`/`subcontractor_cost`/`margin_amount` = **`1d0c937b0cfa7c848c3a8bfb8a6870a2`** · التوزيع `pending_payment` ٩ · `assigned` ٣ · `confirmed` ٢ · `cancelled` ٢ · `under_review` ١ |
| صفَّا الاختبار | **`RQ-ZF83NH`** ⇒ `md5 = f811a94f…` · **`TR-S3GXYJ`** ⇒ `md5 = ad0bcdfe…` — **قائمان بلا حركة** |
| حمزة الغمري | صفٌّ واحد · `md5 = 562f4be0…` · **`approved`** · **تليجرام `6977010427`** |
| قوائم الأسعار | **٢ · كلتاهما `approved`** · `md5 = d06a5a6b…` |
| شريط الشعارات | `select jsonb_array_length(content->'items') … type='logo-strip'` ⇒ **١٠ عناصر** |
| `payment_providers` | **السبعة `false`** — `binancepay` · `nowpayments` · `paymob` · `paypal` · `stripe` · **`test`** · `twocheckout` — **مقيسةٌ قبل الجولة وبعدها** كما يأمر `OWNER-DECISIONS §١` |
| `loyalty_settings` | `enabled=true` · **`1.25`** · `0.05` · `1000` · `10` · **`updated_at = 2026-08-17 07:27:46.063748` بايتاً ببايت** |
| الكوبونات | `discount_settings.enabled = true` (‏بقراره) — و`facts.mjs` يؤكّده |
| `site_settings` | **١١ صفّاً** · `md5 = e8763280…` · و`seo.robots.indexable = false` |
| `locales` | `ar=true/false` · `en=true/true` — **بلا حركة** |
| الترجمات | **٨٩١ إجمالاً** · `en/published` **٨٧١** · `en/draft` **٢٠** |
| المحتوى | `sections` **١٦٠** صفّاً · `md5 = 705d2f84…` · `pages` **٢٣** · `page_revisions` **١٥** |
| السجلّات | `notifications` **١٦١٠** · `loyalty_entries` **١١٤** · `ledger_entries` **١٤٦** |

**وأرقامُ بدر لا عدد الصفوف — بدور `anon` الحقيقي:** **٧٢٠/١٥٠٠ · ١٨٠٠/٢٦٤٠ · ٣٢٥٠/٤٩٥٢**.

**ومتساقطاتي: صفر.** **لم أُنشئ ملفاً واحداً داخل المستودع** — كلُّ أدواتي في مجلد المسوّدات
خارج الشجرة، فلم أشارك في تصادم المسوّدات الذي أصاب جولاتٍ سابقة. و`ls _*` في الجذر ⇒
**لا شيء** (‏الجولات السابقة نظّفت مسوّداتها)، و`git status --porcelain` ⇒ **٠ سطر `D`**.
**ولا كمّ ولا دفع ولا نشر.**

---

## ٧) ⚠ ما لم أستطع التحقق منه — مسمّىً بلا تجميل

1. 🔴 **لا لقطةَ شاشة.** لوح المعاينة رفض (`the Browser pane is not displayed, so the page is
   not compositing frames`). ما عندي **قراءةُ DOM حيّة من متصفحٍ حقيقيّ** (‏`lang`/`dir`/نصُّ
   العقدة/`getComputedStyle`/انعدامُ التجاوز الأفقي) — **أقوى من `curl`، وأضعفُ من عين.**
   ⇒ **بندا التباين (١٢ و١٣) والمظهرُ العامّ لم يرهما أحدٌ في أربع جولاتٍ متتالية.**
2. 🔴 **الجبهةُ الرابعة (‏اتفاقية المتعهد، `0113`) لم أراجعها ولم تكن من مهمّتي.** قِستُ أن
   بوابتها خضراء (‏`partner_agreement_tests.sql` ضمن ٣٩/٣٩، و`tsc` صفر بعد سكونها) —
   **لكنّي لم أقرأ هجرتها ولا صلاحياتها ولا شاشتيها.** **خضرةُ البوابة ليست مراجعةً أمنية.**
3. **لم أُشغّل المجموعة الجديدة `partner_agreement_tests.sql` منفردةً** ولم أقرأ ما تؤكّده؛
   رأيتُ `ALL PASSED` منها داخل الجولة الكاملة فقط.
4. **لم أطبّق هجرةً ولم أُصلح عيباً** — بريفي تحقّقٌ لا بناء، وبلا رقمٍ مُسنَد. ⇒ **البنود ١ و٢
   (‏`TRIGGER` و`DELETE` لـ`service_role`) بلّغتُ عنها فقط، وهما مفتوحان الآن.**
5. **لم أفتح `lib/item-fields-types.ts` ولا `lib/page-builder-types.ts` للكتابة** — خارج
   ملفّاتي، وجبهةٌ أخرى قد تكون فيهما. **فالقراءة الخاطئة عن `href` ما زالت مكتوبةً فيهما.**
6. **`quote_public` عبر `/api/quote` لم أُجبر الخطأ من خلاله** — قِستُ الدالة في القاعدة
   وبدور `anon` مباشرةً. **ماذا تعرض شاشةُ العروض للزائر عند الرفض: لم أره.**
7. **`available_payment_accounts` مع `NaN` تُضيّق قائمة حسابات الدفع** (‏قياسُ الجولة الرابعة،
   **لم أُعِده**). فشلٌ آمنٌ في الاتجاه، **ولم يُصلَح ولم يُرَ أثرُه على شاشة الدفع.**
8. **بنود ١٢ و١٣ و١٦ منقولةٌ بأرقامها من جولاتٍ سابقة** — لم أُعِد قياس التباين ولا التوقيت.
9. **`pnpm db:test` و`pnpm lint` عبر pnpm لم أشغّلهما** — شغّلتُ `node scripts/db-test.mjs`
   و`npx eslint .` مباشرةً (وهو ما يفعله السكربت).
10. **خادمُ `:3000` ليس لي** ولم يكن يستجيب (‏`curl` ⇒ `000`)؛ قِستُ على `:3877` وحده،
    **وأوقفتُ خادمي بعد الفراغ منه.**
11. **الاستشهادُ بموافقة المالك على النصّ الإنجليزي استنتاجٌ من الحالة** (‏`provider=human` ·
    `status=published`)، **لا أمرٌ رأيتُه بخطّه.**

---

## ٨) وبيد بدر وحده

| البند | ما ينتظره |
|---|---|
| 🔴 **رفعُ `noindex`** | **الحواجزُ الثلاثة سقطت، والنافذة ما زالت مغلقة بأمانٍ** (‏`indexable=false` · `Disallow: /` · `noindex, follow` على الصفحتين · خريطةٌ فارغة). ورفعُها يُخرج **٨٧١ صفّاً إنجليزياً** إلى جوجل في اللحظة نفسها — **وقد صار النصُّ صادقاً قبل أن تُفتح** |
| 🔴 **`revoke trigger`** على `audit_log` · `ledger_entries` · `funnel_events` · `loyalty_entries` من `service_role` | بندٌ ١ — **مقيسٌ `true` عند 05:25:40Z**. هجرةٌ برقمٍ مُسنَد (`0114` حرّ) |
| 🔴 **`revoke delete on loyalty_settings`** من `service_role` | بندٌ ٢ — **مقيسٌ `true`**، وقرارُه (‏١٫٢٥ مُشعَل) يُمحى بأمرٍ واحد **بلا طريق عودةٍ من الواجهة** (بند ٨) |
| **`round_trip` و`status`** في نصّ الخصوصية | تُذكران في اللغتين معاً أو تُتركان في كلتيهما — **قرارٌ واحد، لا انحرافَ اليوم** |
| **تصحيحُ ملفَّي `lib/`** عن `href` | جولةٌ مُسنَدةٌ لهما وحدهما، وإلا محا وكيلان أحدَهما الآخر |
| **حذفُ ١٥٦٥ صفَّ إشعارٍ** | بعد `pnpm db:backup`. **بيده هو، والسكربت جاهزٌ وجافّ** |
| **تنظيفُ ٢٣٣٨٣ صفَّ تدقيقٍ مجمَّد** | هجرةٌ مقصودة أم تركُها سنة (بند ١٠) |
| **`--tone-warning`** | «نفّذ» أو «اتركه» (بند ١٢) |
| **Paymob + KYB** 🔴 · `RESEND_API_KEY` · GA4/Clarity/Meta · حصّة Google · إذن OSM | كما هي |
| **«وضع العميل»** و**التأجير بالساعة** | نقاشٌ قبل بناء |

</div>
