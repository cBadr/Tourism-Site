/**
 * رموز الحركة — مرآة TypeScript لما كتبه م‑١ في `app/globals.css` §(ط).
 *
 * لماذا ملفٌ ثانٍ أصلاً؟ لأن نصف الحركة هنا CSS خالص (يقرأ `var(--dur-2)`
 * مباشرةً) والنصف الآخر جافاسكربت يحتاج **رقماً** لا نصّاً: `animate()` من
 * `motion` تطلب مدةً بالثواني ومنحنى بأربعة أعداد. فلو كُتبت القيم في المكانين
 * لانحرفا يوم يغيّر أحدهم `--dur-3` — وهذا بالضبط النمط ٨ في `handover/LESSONS.md`.
 *
 * 🔴 **القاعدة:** هذا الملف يتبع `globals.css` ولا يقوده. أي تغيير هناك يُنقل
 * هنا في نفس الالتزام. وما ليس له نظير في `globals.css` مجمَّع في `DESIGN_ONLY`
 * أدناه صراحةً، حتى لا يظنّه قارئ لاحقاً رمزاً نظامياً فيبني عليه.
 */

/** المدد الثلاث — نفس أرقام `--dur-1/2/3`، بالمللي ثانية. */
export const DUR = {
  /** `--dur-1` — لمسة: لون، حدّ، ضغطة زر. */
  fast: 160,
  /** `--dur-2` — الحركة الافتراضية: ارتفاع بطاقة، انزلاق درج، ظهور وهج. */
  base: 280,
  /** `--dur-3` — الحركات الكبيرة: ظهورٌ عند التمرير، تكبير صورة. */
  slow: 520,
} as const;

/**
 * المنحنيات كأربعة أعداد — الشكل الذي تقبله `motion`.
 * `EASE` هو `--motion-ease`، و`EASE_BACK` هو `--motion-ease-back`.
 */
export const EASE = [0.22, 0.61, 0.36, 1] as const;
export const EASE_BACK = [0.34, 1.4, 0.64, 1] as const;

/**
 * منحنيان يعيشان في التصميم وحده ولا نظير لهما في `globals.css` — عن قصد:
 * كلاهما يخدم حركةً واحدة بعينها، ورمزٌ نظامي لمستعملٍ واحد ضجيج.
 */
export const DESIGN_ONLY = {
  /**
   * منحنى قيادة السيارة على مسار «كيف نعمل».
   * `--motion-ease` مقدَّم الطاقة (يقطع ٨٣٪ من المسار في ٤٥٪ من الزمن) فتبدو
   * السيارة كأنها اندفعت ثم تعطّلت — ولهذا منحنى متماثل مستقل للقيادة وحدها.
   * (نصّ التبرير من `style.css:1553-1556` — منقول لأنه قياسٌ لا رأي.)
   */
  easeDrive: [0.4, 0, 0.6, 1] as const,
  /** منحنى العدّاد: تسارع قصير ثم تباطؤ طويل. (`main.js` تنقيح ٢) */
  easeCount: [0.3, 0.02, 0.22, 1] as const,
} as const;

/** أزمنة الحركات المسمّاة — كلها من التصميم، بالمللي ثانية ما لم يُذكر غير ذلك. */
export const TIMING = {
  /** زمن رحلة السيارة على المسار — يشاركه رسم الخط فتبقى عند رأسه بلا مزامنة. */
  driveMs: 3600,
  /** زمن عدّ الرقم الواحد في شريط الأرقام. */
  countMs: 1600,
  /** الفارق بين ظهور عنصرٍ وتاليه في الظهور المُدرَّج (`--stagger` × هذا). */
  revealStepMs: 70,
  /** الفارق بين بدء عدّاد وتاليه — يوازي تدرّج ظهور البطاقات. */
  countStepMs: 90,
  /** دورة شريط الشعارات = نسخة واحدة. ٤٠ ثانية ≈ ٣٨ بكسل/ثانية. */
  marqueeSec: 40,
  /** دورة Ken Burns على صورة البطل — ذهاباً وإياباً. */
  kenBurnsSec: 20,
  /** دورة جسيمة ضوئية واحدة في البطل. */
  driftSec: 14,
} as const;

/**
 * أدنى عرض شاشة يُحمَّل عنده فيديو البطل.
 *
 * 🔴 **٩٠٠ قرارٌ لبدر لا قيمة معاينة** (`DESIGN-DECISIONS.md` بند ٧): ملف
 * التصميم يحمل `MIN_WIDTH = 0` وحزمته نفسها تسمّيها «قيمة معاينة مؤقتة».
 * تحتها: صفر بايت فيديو على الجوال — وهو الرقم الوحيد المضمون بنيوياً في
 * خطة الأصول كلها.
 */
export const HERO_VIDEO_MIN_WIDTH = 900;
