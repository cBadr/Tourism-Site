# ⛔ قف — اقرأ حزمة التسليم قبل أي شيء

**هذا المشروع له تاريخ طويل من القرارات المحسومة والدروس المدفوعة الثمن.
البدء بالكود قبل قراءة الحزمة يعني نقض قرار أو إعادة اكتشاف عيب سبق إصلاحه.**

## أول إجراء في أي جلسة جديدة — قبل أي رد وقبل أي أداة أخرى

اقرأ بهذا الترتيب:

1. **`handover/INDEX.md`** — نقطة الدخول: يدلّك على ما تحتاجه بحسب مهمتك.
2. **`handover/OVERVIEW.md`** — ما هو المنتج، وأين وصل، و**«ما يجب ألا تكسره»**.
3. **`handover/USER_PROFILE.md`** — كيف يعمل بدر وكيف تتعامل معه.

ثم — **قبل كتابة أي كود** — أضف:

4. **`handover/ARCHITECTURE.md`** و **`handover/LOGIC.md`** و **`handover/CONVENTIONS.md`**
5. **`handover/DECISIONS.md`** (٤٠ قراراً بمبرره وعاقبة نقضه) و **`handover/LESSONS.md`**
   (قائمة فحص تُشغَّل قبل إعلان اكتمال أي مرحلة)

ولا تعتمد على هذا الملف وحده: **`docs/VISION.md` هو المرجع الأعلى للمنتج**
(لا يُحذف منه بند أبداً)، و**`docs/ROADMAP.md` سجل التقدم الحي** الذي يُحدَّث
أولاً بأول. أما `PLAN.md` في الجذر فمسودة **ملغاة** — تجاهلها.

## ثلاث حقائق تكسر الجلسة إن جهلتها

| الحقيقة | الأثر إن جُهلت |
|---|---|
| جذر git كان مجلد المنزل `C:/Users/Badr` قبل 2026-08-12 | `git add` من داخل المشروع بلا التحقق من `git rev-parse --show-toplevel` يرفع `.ssh` و`.claude.json` |
| `DATABASE_URL` **لازم** يكون Session pooler لا المضيف المباشر | المضيف المباشر IPv6 فقط ⇒ كل أوامر الهجرات والاختبارات تفشل بـ ENOTFOUND |
| `NODE_OPTIONS=--max-http-header-size=65536` في سكربتَي dev وstart | حذفه يعيد خطأ HTTP 431 على localhost بسبب كوكيز متراكمة |

## طريقة العمل المتفق عليها

- **وزّع التنفيذ الثقيل على وكلاء متوازين** وأبقِ السياق الأساسي للقرارات
  وكتابة العقود (`lib/*-types.ts`) والدمج والتحقق الحي. التفاصيل وحدودها في
  `handover/CONVENTIONS.md` و`handover/LESSONS.md`.
- **بعد كل مرحلة بناء: مراجعتان نقديتان** (أمنية + انحراف عقود). أمسكتا في
  **كل** مرحلة عيباً حرجاً.
- **لا تُعلن اكتمالاً إلا بعد** `pnpm db:test` (كل مجموعة تطبع `ALL PASSED`)
  و`pnpm build` وتحقق حي في المتصفح.
- **تقارير الوكلاء تُجمِّل وتغفل** — طبّق واختبر بنفسك قبل تصديقها.

---

@AGENTS.md
